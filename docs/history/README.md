# 작업 이력 (`doc/history/`)

날짜별 작업 내역을 누적해 두는 폴더입니다. **하루에 한 파일** 원칙.

## 파일명 규칙

```
history.yyyyMMdd.md
```

예: `history.20260514.md`. 작업이 있는 날만 생성합니다.

## 파일 구조 (공통)

각 파일은 다음 순서를 따릅니다.

1. **한 줄 요약** — 그 날의 가장 큰 변화
2. **번호별 작업 섹션** — 결정 사항 / 코드 변경 / 배포 / 미세 조정
3. **산출물** — 추가·수정된 파일, 배포 버전, 외부 URL
4. **다음 단계 / 알려진 한계**

## 인덱스

| 날짜 | 요약 |
| --- | --- |
| [2026-06-29](./history.20260629.md) | **브랜드 사이트 v5 목업 정본 verbatim 재구축 + 검증 + 배포** — 목업이 시안과 어긋나던 원인(정본 `.dc.html` 1920px 절대좌표를 flex로 재해석)을 잡아, v5 정본 **마크업 그대로 이식** + `BrandCanvas`(`document.body.zoom` fit + 배경 동기화)로 **정본 렌더와 일치**시켜 재구축. 소스 대조(로그인·가격·메인) 일치 확인. BR01 39화면(누락 0)·`/`=화면 카탈로그(ID 표준화)·메인 `/landing`. 검증(오품관): A형(정본 그대로 — 무료체험·인증코드 정본 유지) 선택, 모달 6 미구현. `malgnsoft/solsol-brand`(`f0dc5a5`) → Pages `solsol-brand-mockup` 배포 |
| [2026-06-26](./history.20260626.md) | **주간 요약(06-23~26) + WBS 재구성(퍼블리싱·설계·구현) + /screens 목업 링크 정상화** — `/screens` AD01/BR01 0% 표시는 캐시(프로덕션 정상)로 진단, 잔여 D1 토글 2행 정리(FR01 100% 복원). **WBS 스텝 4·5·6을 「퍼블리싱(목업)·설계·구현」으로 전면 재구성** — 화면영역(FR/AD/BR/BO)×큰 기능 **40항목** 신규 생성(스텝1·2·3·7 보존), 어제 3영역 목업·검증 성과를 스텝4 목업(목업 URL href)·스텝5 검증에 반영. 라이브 D1 `wbs_item` 동기화(스텝4·5·6 DELETE→INSERT) + `wbsData.ts`·`seed.sql` 갱신 |
| [2026-06-25](./history.20260625.md) | **브랜드 사이트(BR01) 전체 목업 + 크리에이터 사용자단(FR01) 목업/검증 + 화면 페이지 D1화** — 에이전트팀으로 ① FR01 46화면 목업(`solsol-mockup.pages.dev`) + 검증(게이트 미통과·blocker5 기록 후 배포) ② 관리 허브 `/screens`(화면) D1 편집형 전환(`screen_status` + GET/PATCH + 클릭토글/URL) + 화면 목록 `00_화면목록.md` v1.2 재생성(본화면+실제 `_pu`/`C##` 모달) + FR01 목업 링크 ③ **브랜드 사이트(BR01) 39화면 + 모달 6 전체 목업**(핸드오프 v2, Nuxt 3+Tailwind+Nuxt UI) + 검증 **게이트 통과**(❌상 0·누락 0, major 4: 무료체험 문구·인증코드 3→10분·성/이름 2단·카피) + `solsol-brand`(`349ba9e`) → Pages `solsol-brand-mockup` 배포 ④ CLAUDE.md 목업 검증 절차(라운드) 추가 ⑤ **크리에이터 관리자단(AD01) 96페이지 전체 목업**(핸드오프 Nuxt SFC 스캐폴드 통합) + 검증(본화면 107 **전수 생성**·게이트 미통과 모달 blocker2 보완대기) + `solsol-admin`(`5e44ea8`) → Pages `solsol-admin-mockup` 배포 |
| [2026-06-23](./history.20260623.md) | **쏠쏠 브랜드 사이트 화면 목업 제작 + 배포** — Figma "03. Brand site 확정본"(레드/Pretendard) 기준으로 브랜드 사이트(사용자단) 전체 25개 페이지(메인·가격·문의·인증·결제·구독·마이페이지·상태·약관·404)를 정적 HTML 목업으로 제작 → `solsol-brand/mockup/`(토큰·공용 헤더/푸터·갤러리 인덱스) + `malgnsoft/solsol-brand` 첫 푸시 + Cloudflare Pages `solsol-brand-mockup` 배포(<https://solsol-brand-mockup.pages.dev>). Figma MCP 호출 한도로 미열람 화면은 확정본 패턴 기반 재구성 |
| [2026-06-17](./history.20260617.md) | **참여자·이슈 게시판·주간 작업 신설 + 현황판 제거** — `malgn-noti-mng` 구조 참고. 회원 시스템(로그인/가입(승인)/내정보/참여자관리, HMAC 세션 + PBKDF2)으로 단일 비밀번호 게이트 교체 + 이슈 게시판(`/issues`, 정책·이슈·공지·논의 + 댓글 + 마크다운 + R2 이미지 첨부) + 주간 작업(`/weekly`, WBS 파생·Step 1~7 그룹) 신설 + 현황판(`/board`·`/api/board`·board 스키마) 제거 후 대시보드 '프로젝트 현황'을 WBS 요약 + 최근 이슈로 재구성 + 원격 D1 `0002` 마이그레이션(board DROP, member/issue/comment CREATE, `wbs_item` 무손실)·R2 `solsol-mng-files`·세션 시크릿 적용 후 프로덕션 배포 |
| [2026-06-10](./history.20260610.md) | **쏠쏠(SolSol) 리브랜딩 + Cloudflare 연결 + 현황판·WBS 전면 재작성** — `malgn-noti`/`malgn-helper` → `solsol`/`쏠쏠` 전체 치환(코드·docs·로고 message 제거) + 전용 D1 `solsol-project`·Pages `solsol-mng` 신설 + 기획 스프레드시트(이미지 7장) 분석 → 크리에이터 LMS 현황판(7단계·78작업)·WBS 간트(5트랙·32항목, 누락된 `wbs_item` 시드 추가) 새로 작성 + 진척률 dev `/progress` 실측(종합 베타 44%) 정렬 + WBS 마감 8월말 연장 + `/wbs` UX(푸터 제거·스크롤 시 GNB·헤더 접기·기준일 yyyy.MM.dd·클릭 시 오늘로 이동)·WBS 전체 일정 7단계 확장(62항목, 1~8월) |
| [2026-06-05](./history.20260605.md) | **solsol-mng 관리 레포 신설** — GitHub 연결 + `solsol/doc/` 트리 집약(공통·도메인·이력) + CLAUDE.md 병합·현행화(이력 작성처를 본 레포로 변경, solsol `doc/history/` 삭제) + solsol 동일 스택(Nuxt 3 + Tailwind v4 + Nuxt UI v3) + `@nuxt/content` 기반 **문서/이력 브라우저 앱** 스캐폴딩(대시보드·문서·이력 타임라인) + **Cloudflare Pages 정적 배포**(<https://solsol-mng.pages.dev>, 프리렌더 라우트 직접 열거로 마크다운 내부 링크 크롤·대소문자·파일명 점 이슈 해결) + **쏠쏠 현황판(`/board`)** — solsol `/wbs` 읽기 전용 이식(공개 API `GET /wbs` 라이브) + 대시보드 WBS 현황 요약(`AppWbsOverview` 공유) |
| [2026-05-11](./history.20260511.md) | 프로젝트 착수 — 세 레포 부트스트랩 + 첫 GitHub 푸시 + Cloudflare 첫 배포 + 시안 GNB 도입 |
| [2026-05-12](./history.20260512.md) | 시안 정밀 매칭 — 콘텐츠 폭 1200px 통일, Utility Bar 분리, 메뉴 8개, 문서 3종(`FRONTEND`/`STACK`/`DESIGN`) |
| [2026-05-13](./history.20260513.md) | Smart Placement + 발송 페이지 풍부화 — 공용 컴포넌트 21종, `useChannelMeta`, SMS/알림톡 발송 페이지 |
| [2026-05-14](./history.20260514.md) | GitHub 일괄 푸시 + `doc/history/` 폴더 정리 |
| [2026-05-18](./history.20260518.md) | 디자인 시스템 전면 피벗 — Relay-inspired(핸드오프 정본), 셸+홈+발송 전 채널 적용, `design-system-pivot` 푸시 |
| [2026-05-19](./history.20260519.md) | 디자인 가이드 페이지(`/guide`, 18섹션 라이브 카탈로그) + Cloudflare Pages 프로덕션 배포 |
| [2026-05-20](./history.20260520.md) | 발송 페이지 UX 폴리시 2차 + PUSH 부가항목·플로우 관리 + 문서 현행화 + 발송 조회 페이지 재작업·btn-sky 제거 + 통계 페이지 재구성(Chart.js)·폰트 토큰화·zoom 제거 + 발송 템플릿 토글 개선 + 주소록 관리 페이지 강화 + 발신 번호 관리 페이지·등록 마법사 + 그룹 관리 페이지 + RCS 브랜드 관리 페이지 + 이메일 도메인 관리 페이지 (배포 #15~#26) |
| [2026-05-21](./history.20260521.md) | 발신 정보 페이지 마무리(발신 프로필·PUSH 인증·080 수신 거부) + 테이블 스타일 A/B/C 정의·발송 조회 툴바 재배치 + GNB 정리 + 수신 거부 관리 3종 + 문자메시지·알림톡 템플릿 관리 페이지 + 미리보기 시각 현재화 + 로그인 페이지 시안 IA 재구성 + 비밀번호 재설정 입력란 보정 + 메시지 관리 5채널 템플릿 페이지(SMS·알림톡·RCS·이메일·PUSH) + 보안 인증 페이지 보정 + 사이트맵 페이지 + 회원가입 5단계 마법사 + 메시지 상세 설정 페이지 + 새 비밀번호 설정 페이지 보정 + 랜딩페이지 만들기 + 문의하기 페이지 /account/inquiry 이동 + 나의 페이지 섹션 + 크레딧 충전 플로우 + API 루트 → /doc 리다이렉트 (배포 #27~#46 + API) |
| [2026-05-22](./history.20260522.md) | 나의 페이지 화면 신규 구성 — 비밀번호 변경·보안로그인 설정·멀티 계정 추가·계약 관리(계약서 확인·3스텝 전자서명 위저드·PDF 미리보기) + 크레딧 관리(A형 내역 테이블·영수증 모달) + 나의 문의(목록·상세 댓글 스레드)·문의하기 LNB + 서비스 담당자 초대 플로우(/invite 등록 페이지)·문의 등록 완료 페이지 + 사이트맵 현행화 + 운영 가이드 페이지(/help)·GNB 연결 + 비로그인 공개 랜딩 페이지(/, 히어로·장점·채널 단가 비교) (배포 #47~#52) |
| [2026-05-26](./history.20260526.md) | `solsol-api` 데이터 모델·초기 DDL·Hyperdrive 연결·첫 프로덕션 배포 — 49 테이블 데이터 모델(TB_·company_id·status INT 1/0/-1 + *_state·*_yn·loginid/email 분리) + Mermaid ERD 9종 + 확장성 전략(월 RANGE 파티셔닝·Hot/Warm/Cold·R2 오프로드) + 0000_initial.sql(49 테이블, 파티션 5종) + Hyperdrive(`a2ba4efe...`) 바인딩 + drizzle-orm/mysql2 + /health/db + `wrangler dev --remote`로 로컬도 실제 Aurora + `https://solsol-api.malgnsoft.workers.dev` 프로덕션 첫 배포(/health/db → mysql_version 8.0.42) + `solsol-api/CLAUDE.md §8.1`에 배포·Git·작업 이력 운영 컨벤션 명문화 + solsol 프론트 배포 #53 (list 툴바 새로고침 버튼 일괄 제거 + guide/DESIGN 갱신) + `/admin/migrate`·`/admin/tables`·`/admin/partitions` 라우트 + 0000_initial.sql Aurora 적용 (49 테이블 + 75 파티션 라이브) + 기본 CRUD API 골격 (errors/pagination/auth/Drizzle schema + /me /contacts /contact-groups /sender-phones) + `/doc` API 가이드(Scalar UI + OpenAPI 3.1) + solsol-api 프로덕션 배포 #2 (Version `1fdc3b12...`, 가드 작동 확인) + 14개 도메인 라우트 추가(발신정보 5종/optout-entries/templates 2종/inquiries/company-settings/payment-methods/landing-pages/credit-ledger) + Phase 1·2·3 (/doc 확장 paths 10→37·schemas 11→45 + signup/login + PBKDF2 + JWT HS256 + 발송 이력 read-only with 90일 윈도우 강제) + solsol-api 프로덕션 배포 #3 (Version `926017d2...`, JWT_SECRET secret, 실제 /auth/signup → /me 동작 확인) + POST /send/sms 발송 producer(DB 적재까지 — 발신번호 검증·옵트아웃 필터·크레딧 hold·트랜잭션, 알려진 멱등 버그 TODO) + solsol-api 프로덕션 배포 #4·#5 (Version `4d9e1fbe...` → `afaa4c89...`, /send/sms 라이브 가드·검증 흐름 9건 + 재배포 검증 4건, jwt.ts linter 캐스트 명시화) + 🐛 멱등 버그 해결 (TB_IDEMPOTENCY + INSERT-then-conflict race-free 패턴, 검증 3건 통과) + NHN SMS 어댑터(mock/real) + Cloudflare Queues(`solsol-dispatch`) + consumer worker (dispatch_state 천이) + 프로덕션 배포 #6 (Version `b30dc2a3...`, Producer+Consumer 동시 바인딩 라이브, 큐 e2e는 Cloudflare 1105 회복 후 재검증) |
| [2026-06-04](./history.20260604.md) | **§1** NICE IPv6 진단 — 6/2 §16의 1007 차단 후속. 사용자 옵션 B(NICE 콘솔에 Cloudflare egress IP 대역 등록) 진행 후 재시도 → 여전히 1007. 진단용 `/diag/egress` 임시 endpoint로 `api.ipify.org` 8회 호출 → Workers outbound가 **IPv6 `2a06:98c0:3600::103`** (Cloudflare 공식 IPv6 `2a06:98c0::/29` 소속)임을 확인. NICE에 IPv4만 등록되어 IPv6 출발지 거부가 원인. 사용자에게 Cloudflare IPv6 대역 7개 추가 등록 또는 IP 검사 OFF 안내 후 보류. 진단 endpoint 즉시 제거 + Workers 재배포. CLIENT_ID/SECRET 덮어쓰기(값 일치 보장만). **§2** Hyperdrive 교체 — `a2ba4efe...` → `439b109d...`. 신규 origin host `malgn-dev-db.apiserver.kr` (Cloudflare Tunnel 엔드포인트) + `access_client_id`. Aurora 노출이 "퍼블릭+SG egress IP 화이트리스트"에서 "Tunnel(Access) 기반"으로 전환 — egress IP 동기화 부담 해소, CLAUDE.md §12 TODO "SG 갱신 운영 절차" 항목 자연 달성. 코드 변경 0. 라이브 검증(GET /health/db mysql_version 8.0.42 + POST /auth/nice/init DB 쓰기 정상). 정본 3개 동기화 — API CLAUDE.md §3·§8·§12, SCALABILITY.md §6 신규 절(전/후 비교 + 전환 이유 + 운영 영향), MIGRATION.md §1 통로 다이어그램에 Tunnel 단계 추가. Workers 배포 Version `a457b7dc...`. **§3** 관리자단 핸드오프 풀세트 — `handoff_noti_admin` (3,129줄 jsx)를 Vue 3 + Nuxt UI v3로 1:1 포팅. 셸 완전 재정비(AppLnb 핸드오프 정본 9 그룹/17 라우트로 교체 + AppTopbar useState 기반 동적 브레드크럼 + useBreadcrumb composable) + 공유 컴포넌트 14종(PageHeader/SectionCard/Tabs/Segmented/FilterBar/DateRange/DataTable generic+`#cell-{key}` scoped slot/Pagination/StatusBadge 값→톤 자동 매핑/ChannelChip/StatCard accent 7종/Drawer/Modal Teleport+scroll lock/Field/EmptyState) + 차트 4종(BarChart 그룹 막대/AreaChart SVG path+그라데이션/Donut stroke-dasharray/ProgressBar) + **17 페이지**(대시보드·고객사·고객사 상세·계정·모니터링·발신번호·발신프로필·템플릿검수·결제·채널단가·충전쿠폰·1:1문의·FAQ·공지·통계·운영자·권한그룹·API). app.config.ts `info: 'indigo'` 매핑으로 핸드오프 indigo 강조색을 Nuxt UI semantic으로. 18 라우트 라이브 200. **§4** 폰트 사이즈 정합화 — 사용자 보고로 두 원인 동시 발견: (a) main.css `html,body{font-size:13px}`가 Tailwind 토큰(16px base 가정)을 모두 18% 축소시킴 — 핸드오프는 base font-size 명시 없이 16px Tailwind 기본 사용 + `letter-spacing: -0.01em`. (b) 직전 turn의 cwd가 `/solsol`(사용자단)였고 거기서 빌드한 dist를 `wrangler pages deploy --project-name=solsol-admin`으로 **사용자단을 admin 프로젝트에 잘못 배포**한 상태였음(chunk 600개 / AppGnb·AppContractPanel·AppCardAddDialog 등 사용자단 chunk 혼입). 둘 다 정정 — main.css 13px 제거 + letter-spacing -0.01em 추가, admin 디렉토리에서 clean rebuild + 재배포 alias `8852d5da.solsol-admin.pages.dev` (chunk 96개로 정상화). 교훈으로 멀티 레포 deploy 가드(prebuild name 체크 + chunk 수 sanity)·base font-size 명시 금지 패턴 정리. solsol-admin 커밋 `0227cae` + `1b63200`. **§5** WBS 페이지 편집 — DB 미사용/R2 단일 JSON 객체 정본(`wbs/wbs.json`, FILES 바인딩). API에 GET 공개 + PATCH 인증 2 라우트 + 142 task 시드(`src/data/wbs-seed.ts` + `src/routes/wbs.ts`). 사용자단은 임베디드 STAGES 제거 → top-level `await api('/wbs')` + AppModal 인라인 편집 다이얼로그(owner·note·href·targetDate·completionDate 5 필드, 빈값=`null`=필드 제거). 비로그인은 읽기 전용 + "로그인하면 편집 가능" 힌트. 동시 편집은 last-write-wins. solsol-api 커밋 `9945db3` Workers Version `28f3e6a8...`, solsol 커밋 `3ed473e` Pages alias `02bb58e6`. 후속(§5.9) 목표일·완료일 `YYYY.MM.DD` 포맷 통일 + `<input type="date">` 캘린더 위젯 + API Zod regex `^\d{4}\.\d{2}\.\d{2}$` 강제. 레거시 `5/8` 값은 표시 시 2026 기준 `2026.05.08`로 자동 변환, 다음 PATCH 시 R2에도 정합화. solsol-api `3a35464` Workers Version `eb02206c...`, solsol `08e5c33` Pages alias `98bd09e2`. **§6** NHN Notification Hub OAuth 어댑터 + Email 실 발송 + 서비스 담당자 이메일 변경 라우트 — Notification Hub 신규 통합 서비스의 인증이 OAuth2 client_credentials → Bearer 토큰임을 확인 후 어댑터 전면 재작성. `src/adapters/nhn/oauth.ts` 신규(`https://oauth.api.nhncloudservice.com/oauth2/token/create` Basic Auth + `scope=appKey:{APP_KEY}` 토큰 발급 + 메모리 캐시). SMS·Email 어댑터 `POST {base}/message/v1.0/{SMS|EMAIL}/free-form-messages/{purpose}` + `contactType=PHONE_NUMBER`/`EMAIL_ADDRESS` + `X-NC-APP-KEY` + `X-NHN-Authorization`. `NhnCredentials` 확장(userAccessKey/secretAccessKey + legacy secretKey 옵셔널). Email은 `message@malgnsoft.com` 발신 도메인 NHN 콘솔 등록 + `EMAIL_FROM`/`EMAIL_FROM_NAME` secret 등록 후 실 발송 검증 통과(`messageId=20260604154608lR6MBAKn8v0`). 동시에 `POST /me/email-change` 신설 — 비밀번호 + OTP(`purpose=change_email`) 검증 후 **`user.email` 만** UPDATE(`loginid` 가입 시 식별자로 고정 유지). 사용자단 `AppEmailChangeDialog` + auth store `changeEmail()` 실 API 연동. **§7** WBS 현행화 — R2 정본(`wbs/wbs.json`) + `doc/WBS.md` 동시 갱신. Step 5 진척 48% → 55%, 가중평균 약 47.5%. 9 신규 task(5-2-19/20/21 WBS R2 / 이메일 변경 / NHN OAuth 어댑터, 5-3-15 `/wbs` 인라인 편집, 5-3C-20 이메일 변경 다이얼로그, 5-4-14/15/16 핸드오프 17 페이지 + dev 라벨 + 관리자 로고, 5-5-10/11/12 Hyperdrive Tunnel / NHN Email real / NHN SMS pending) + 5-3C-7 완료 승급 + 5-2-16·5-5-5 in_progress 승급. 추가로 사용자 요청으로 김도형 담당 done은 t=c 통일, pending·in_progress 36건은 7/3 마감으로 W1~W5 일정 분배(R2 + doc/WBS.md 동시 적용). **§8** 서비스 담당자 이메일 변경 — 세 가지 버그 동시 fix. (a) `/auth/email-code/verify` 가 `purpose='change_email'` 에서 `consumed_at` 마킹 → 직후 `/me/email-change` 의 OTP 재검증 실패하던 이중 소비 버그를 verify-only 분기로 수정. (b) 비밀번호·OTP 오류 401 → `useApi.onResponseError` 자동 로그아웃 트리거 → 새 헬퍼 `errors.unprocessable()`(422) 도입, 본인 재인증 실패 코드 분리. (c) 다이얼로그 `submit()` 데드락(부모 close 대기) → `onConfirm: (p) => Promise<void>` 콜백 prop 패턴 전환, 다이얼로그가 자기 상태 직접 관리(성공·실패 모두 잠금 해제). solsol-api Workers Version `772adb3b...`, solsol Pages alias `cd6a5bb3`. **§9** 광고성 메일 수신 동의/거부 일시 기록 — DDL 0006 (`TB_COMPANY.ad_receive_at DATETIME NULL`) Aurora 직결 적용 + `PATCH /me/company` 에서 `adReceive` 변경 시 `adReceiveAt: new Date()` 동시 set + `readContext` 4 응답 라우트(GET /me · PATCH /me · PATCH /me/company · POST /me/email-change) 모두 포함. 사용자단 패널에 `[🕒] YYYY.MM.DD HH:mm 광고성 메일 수신에 동의/거부함` 회색 칩 노출 + 토스트 description 처리 일시 표기. 정보통신망법 50조 동의 증빙용. solsol-api Workers Version `3671ce95...`, solsol Pages alias `0f43b158`. |
| [2026-06-02](./history.20260602.md) | 회원·인증 트랙 첫 날. **§1** WBS 구조 개편 — 사용자단을 **5-3A 화면 UI 구성**(목업) + **5-3M 매트릭스**(도메인별 UI/API/연동 한눈에) + **5-3C 화면 ↔ API 연동**(실 데이터) 3 트랙으로 분리. 5-3-15 단일 항목을 16개 도메인별 5-3C-1~16으로 펼침. Step 5 진척률 55%→40%, 전체 가중평균 45%→38%. Pages 배포 #51(alias `bca573ce`). **§2** 로그인 UX 개선 — `POST /auth/login-by-email` 신설(이메일+비번 → 회사 자동 찾기, 단일 매치 즉시 토큰 / 복수 매치 시 `multipleCompanies:true + companies[]`). 프런트 `login/index.vue`에서 **고객사 ID 필드 완전 제거**, 복수 매치 시 회사 선택 카드 UI. Workers 배포 #10(Version `a6197cc7...`), Pages 배포 #52(alias `292da05d`). 라이브 e2e 5 시나리오 통과. WBS 5-3C-4 ⚪→✅. **§3** `TB_USER.loginid` 전역 UNIQUE 정합화 — 정책상 한 이메일이 여러 회사에 가입되어선 안 됨. `0003_user_loginid_global_unique.sql` 신규 + 라이브 적용(`uq_user_company_loginid` 복합 DROP → `uq_user_loginid` 단독 ADD). schema.ts에 `.unique('uq_user_loginid')` 명시. login-by-email 단순화(`for-of` 다중 verify 루프 제거, `.limit(1)`) + OpenAPI에서 `MultipleCompaniesResponse` 삭제 + 프런트 회사 선택 UI(~80줄) 제거. 사전 검증용 임시 데이터 cleanup(회사 8 + 사용자 12). Workers 배포 #11(Version `f7f42855...`), Pages 배포 #53(alias `f150ea0a`). 라이브 e2e 4건 통과(signup 정상 / loginid 중복 409 / login-by-email 단일 토큰 / UNIQUE 인덱스 단독 확인). "한 이메일 = 한 회사 = 한 로그인". **§4** 휴대폰 SMS OTP 라우트(`/auth/phone-code/send`·`/verify` 이메일 OTP와 동일 패턴, `target_type='phone'`) + signup.vue Step 4 실 API 연동 + useApi.ts 401 처리 분리(`/auth/*` 호출은 호출자 처리 — 가입 도중 OTP 잘못 입력 시 페이지 이동 버그 수정) + 가입 완료 화면 고객사 ID 노출 제거 + 토스트 위치(오른쪽 위) + 크기 강화(17px·440px). Workers #12(`84056c86...`), Pages #54~#58. **§5** NICE 통합인증(휴대폰 본인확인) 인프라 — doc/NICE_AUTH.md 신규 정본(12 섹션) + `0004_user_nice_auth.sql` 라이브 적용(TB_NICE_AUTH 신설 + TB_USER에 birthdate/gender/national_info/ci/mobile_co + UNIQUE ci) + NICE 어댑터(mock/real, AES-256-GCM + PBKDF2 + HMAC, Workers Web Crypto만) + 3 라우트(init/callback/status) + /auth/signup 확장(niceSession 검증·CI 중복 차단·NICE 결과로 이름·휴대폰·생년월일 덮어쓰기·state='consumed' 재사용 차단) + signup.vue Step 4 통째로 NICE 흐름으로 교체("본인 인증하기" 버튼 + popup + 폴링 + 결과 표시) + NICE_MOCK secret 적용(자격증명 발급 전 mock 통과). Workers #13(`2ab47c1f...`), Pages #60(`c9577894`). 라이브 e2e 6 시나리오 통과. **§6** `/account/settings` 실 API 연동 — 백엔드 `GET /me` 응답을 TB_USER 13 + TB_COMPANY 14 컬럼 풀로 확장 + `PATCH /me`(name·phone) + `PATCH /me/company`(companyPhone·billingEmail·adReceive, owner/admin) 신설. 사용자단 `AppMemberInfoPanel.vue` 전면 교체 — 목업 데이터 제거 + useAuthStore 기반 실 데이터 + 광고수신 토글 즉시 PATCH + 저장하기는 변경 필드만 한 번에 PATCH. Workers #14(`22368d14...`), Pages #61(`ea35651d`). 라이브 e2e 5건 통과. WBS 5-3C-7 ⚪→🟢. **§7** 사업자등록증 심사 승인 게이트 — 새 정책: 법인/개인사업자 가입 후 승인 받아야 서비스·정보 수정 가능, 개인은 즉시. 0005 라이브 적용(TB_COMPANY에 company_type/approval_state/rejected_reason + 기존 5행 'approved' 호환). signup 자동 분기 + PATCH /me·/me/company에 승인 게이트 403 + /me 응답에 승인 정보 노출. 사용자단 — signup.vue Step 5 분기 + AppMemberInfoPanel에 승인 배너(pending=warning, rejected=danger+사유) + 모든 입력 disabled. Workers #15(`6e47d50b...`), Pages #64(`56e94e5b`). 라이브 e2e 8 시나리오 통과(법인 pending / 수정 403 / 개인 통과 / 운영자 승인 후 통과 / 반려 사유 노출). WBS 5-3C-6 ⚪→🟢 + 5-3C-17 ✅. **§8** 승인 게이트 전 도메인 일관 적용 — `requireApproved()` 미들웨어를 `src/middleware/approval.ts`로 추출 + 18 도메인 라우트에 일괄 적용(`mutate-only` 옵션 — GET 통과, POST/PATCH/PUT/DELETE만 차단). `/inquiries`는 예외(승인 관련 문의 가능). 자동화 스크립트(grep + perl)로 18 라우트의 import + use 라인 일관 갱신. Workers #16(`798bf6f5...`). 라이브 e2e 6 시나리오 통과(GET 통과 / POST 4건 403 / 개인 통과 / 문의 작성 차단 안 됨). **§9** 사용자단 승인 게이트 UI 일관화 — `AppApprovalBanner` 신규(layout 최상단·GNB 위 글로벌 띠, pending=warning·rejected=danger+사유) + `middleware/approval.global.ts` 신규(차단 페이지 접근 시 `/account/settings`로 리다이렉트, 허용은 `/account`·`/home`·`/help`·`/guide`·`/wbs`·`/inquiry`) + `/home` 미승인 분기(KPI/채널 대신 `approval-hero` 큰 안내 카드 + CTA). Pages #65(`2eec9e0b`). **§10** 미승인 진입점을 `/account/contract`(계약 관리)로 변경 — 정책상 사업자등록증 제출/재제출 화면이 미승인 흐름의 메인. middleware ALLOWED_PREFIXES에서 `/home` 제거 + 리다이렉트 대상을 `/account/settings` → `/account/contract`. AppApprovalBanner CTA "계약 관리". signup.vue Step 5 사업자/개인 분기("계약 관리로 이동" / "대시보드로 이동"). /home.vue §9의 미승인 분기 코드 제거(미들웨어가 차단해 진입 불가). Pages #66(`5256d66d`). **§11** `/account/contract` 실 API + R2 첨부 인프라 — R2 bucket `solsol-files` 생성 + `wrangler.toml` FILES 바인딩 + `schema.ts`에 라이브 DDL 매칭(`TB_CONTRACT`·`TB_CONTRACT_FILE`) + signup corp/sole 분기에서 'initial' 이용계약 자동 생성. `/contracts` 라우트 신규 5종(list · :id/sign · files list · files upload(multipart) · files download(stream) · files delete) — PDF·10MB 제한, name 접두사(`사업자등록증_…`·`대부업등록증_…`·`지급이행보증보험증권_…`)로 종류 구분(`TB_CONTRACT_FILE`에 kind 컬럼 없음), 회사 단위 권한(companyId 매칭), renew 체결 시 다른 done 자동 expired. OpenAPI 5 paths + 2 schemas. 사용자단 `AppContractPanel.vue` 전면 교체 — 목업 제거 + `await Promise.all` SSR + FormData 멀티파트 업로드 + 미리보기는 인증 fetch → blob → object URL(iframe Auth 헤더 우회). Workers #17(Version `7213946f...`), Pages alias `9808fe42`. 라이브 e2e 4 시나리오 통과(corp signup auto-contract / 파일 업로드 R2 / 파일 목록 / 체결 → done+expires +2y). **§12** 사업자등록증 첨부 시 회사 승인 상태 'reviewing' 자동 전이 — `approval_state` enum 4단계 확장(`pending → reviewing → approved | rejected`, DDL 변경 없음 — `VARCHAR(20)`). 백엔드 `POST /contracts/files` kind=biz 업로드 후 회사 상태가 `pending`/`rejected`이면 `reviewing`으로 UPDATE(rejected_reason은 그대로). `requireApproved` + `me.ts` PATCH 차단 메시지에 reviewing 분기 추가. 사용자단 `AuthCompany.approvalState` 타입 확장 + `AppApprovalBanner`·`AppMemberInfoPanel` 3분기 안내문 + `AppContractPanel` 패널 상단 상태 카드(pending=warning · reviewing=info · rejected=danger) 신규. `pickFile` 성공 후 `auth.fetchMe()` 호출 → 글로벌 띠·페이지 배너 즉시 "심사 중" 전환(새로고침 불필요). Workers #19(Version `f6877b91...`), Pages alias `d9a82bfa`. 라이브 e2e 통과(corp 가입 직후 pending → biz 업로드 → reviewing + PATCH /me 403 메시지 변경). **§13** /account/contract 첫 진입 회복 — §11/§12 배포 이전 가입자(회사 16) 두 race 보고에 따라 `GET /contracts` lazy auto-create(corp/sole + 계약 0건이면 'initial' 자동 INSERT) + `GET /contracts/files` 자동 회복(회사 pending + biz 파일 1건 이상이면 reviewing UPDATE) 추가. 회사 16은 즉시 DB UPDATE로 backfill. 프런트 `AppContractPanel` SSR fetch 실패 swallow + onMounted 재시도. Workers 패치 `35e2ec85` → `456b73c2`. **§14** 사업자등록증 파일 행 상태 배지 + 반려 시 삭제(Pages `7675ce8f`) — 회사 approval_state를 모든 biz 파일 행에 동일 배지(reviewing=info·approved=success·rejected=danger) + rejected에서만 빨간 outline 삭제 버튼 노출. 삭제 후에도 rejected 유지, 새 파일 첨부 시 §12 자동 전이 재발동. **§15** 계약 서명 다이얼로그 — 휴대폰 본인인증 sub-step 신규 + 공인인증서 탭 제거. 백엔드 `phone-code` purpose에 'contract_sign' 추가(기존 SHA-256/TTL/5회 제한 인프라 재사용), 다이얼로그 STEP 3에 본인인증 카드(휴대폰 마스킹 + 발송 + 6자리 확인) → 통과 시 success 톤 + 캔버스 셋업. 사용자단 피드백 후속으로 다이얼로그 open watcher에 auth.fetchMe() 강제 hydrate + 공인인증서 탭/CSS·signTab·certLoaded 100여 줄 제거. 정보 테이블 사업자명/대표자 동적 바인딩(`auth.tenant.{name,bizNo,ceoName}`). Workers `85de422a`, Pages `38d4e40e` → `573a6200`. **§16** 운영 노트 — NICE 자격증명 production 등록 시도 → 1007 "허용되지 않은 IP 접근"으로 mock 복귀(secret 3개는 보관). NHN Notification Hub 자격증명 확인 → 신규 통합 서비스가 기존 채널별 NHN과 완전히 다른 인증(`Authorization: Bearer` + OAuth2 client_credentials) → User Access Key 발급 + 어댑터 재작성 필요로 보류. 둘 다 사용자 콘솔 작업 대기. |
| [2026-06-01](./history.20260601.md) | **§1** WBS 정본화 — `doc/WBS.md` 신규(5 단계 가중치·Step 1~5 작업 내역·알려진 한계, ~220 라인) + 사용자단 `/wbs` 공개 라이브 카탈로그(`solsol/doc/WBS.md` 양식 + `solsol-pms/pages/wbs.vue` Notion soft SaaS 디자인 차용 — Hero stats·단계별 진행률 오버뷰·Stage 상세·그룹별 작업 카드·상태 칩·외부 링크 + `layout: blank`·`auth: false`, ~650 라인). Step 5(서비스 개발)는 원본 채널·도메인 단위 항목(대부분 0%)을 실제 진행에 맞춰 5-1 설계(7) / 5-2 API(16) / 5-3 사용자단(15) / 5-4 관리자단(13) / 5-5 통합·배포(7)의 5 그룹 58 작업으로 재정렬. Cloudflare Pages 배포 #47 (alias `0ecc825e.solsol.pages.dev`). **§2** `0002_export_flow.sql` DDL — `wrangler dev --remote` 1105 잔류 3회 재시도 모두 실패(Ray ID `a04a8ca2...`·`a04a9753...`) → Aurora 직결(`noti` 계정·SSL REQUIRED)로 우회 점검. 4 신규 테이블(TB_EXPORT_JOB / TB_FLOW_*) **이미 라이브 적용된 상태 확인** — 컬럼 100% 일치, 인덱스·FK는 라이브가 더 정교(FK 6 + 의미 인덱스명). 라이브 워커 e2e 검증(`/export-jobs`·`/flow-definitions` GET 200 / POST 201) + 테스트 데이터 cleanup + `0002_export_flow.sql` 파일을 라이브 정본에 맞춰 동기화. WBS 5-2-11/12 🟢 (CRUD ✅·처리 worker 미) + 5-5-4 ⛔→✅. **§3** `schema.ts` 정합화 — export/flow 4 테이블에 라이브 정본의 인덱스 6 + FK 6 명시화(`solsol-api: 0475bd2`, schema.ts 단일 파일 +22 -4, 컬럼 정의 변경 0, 다른 테이블 변경 0, typecheck 통과, 런타임 영향 0). drizzle-kit introspect drift 12건 → 0. **§4** 사용자단 인증 백엔드 연동 — `/auth/signup` · `/auth/login` · `/me` 실 API 연동(JWT `auth-token` 쿠키 + `useApi()` Bearer 자동 주입 + 글로벌 미들웨어 1차 가드 + `plugins/auth.client.ts` 클라이언트 부트스트랩 페치). 회원가입 Step 4 → 실 API → 자동 로그인 → `/home`. 로그인은 `last-company-id` 쿠키 자동 사용. 첫 SSR 시도에서 `await auth.fetchMe()`가 Nuxt instance 컨텍스트 손실로 500 → 미들웨어/플러그인 분리로 우회. 6 파일 수정 + 1 신규(`plugins/auth.client.ts`), typecheck 통과, 로컬 + 프로덕션 e2e(쿠키 동봉 시 `/home` 200 · 없으면 `/login?redirect` · `/login`·`/signup`·`/wbs` 200). Pages 배포 #49 (alias `9be4ff61.solsol.pages.dev`). WBS 5-3-15 ⚪ → 🟢. **§5** 이메일 OTP 인증 — `POST /auth/email-code/send`·`/verify` 백엔드 신설(TB_VERIFICATION + SHA-256 코드 해시 + TTL 10분·재발송 시 직전 코드 만료·5회 시도 제한·소비 후 재사용 차단), Drizzle schema.ts에 verification 정의(라이브 정본 정합), OpenAPI 2 paths + 3 schemas, Workers 배포 #9(Version `83f32a61...`). 프런트 signup.vue `sendIdCode`/`confirmIdCode` 실 API 연동(`mockCode` 토스트 노출은 NHN_MOCK=1 한정 — production 자동 차단, 로딩 상태 + 재발송 라벨). Pages 배포 #50(alias `c2100890`). 라이브 e2e 6 시나리오 모두 통과(발송·잘못된 코드 401·올바른 코드 200·소비 후 재시도 401·재발송·DB 행 검증) + 검증 후 임시 NHN_MOCK secret 제거 확인. doc/SIGNUP.md §8 #4 ⚪→✅. |
| [2026-05-27](./history.20260527.md) | **트랙 A** — `solsol-admin` Nuxt 3 + Nuxt UI v3 부트스트랩 + LNB(256px · 8그룹) + TopBar(64px) 셸 레이아웃(`design_handoff_customer_detail` 정본 참조) + 첫 실 Nuxt 앱 프로덕션 배포(`https://solsol-admin.pages.dev`, 이전 정적 placeholder 대체). **트랙 B** — `solsol-api` §19 멱등 버그 정식 해결(TB_IDEMPOTENCY + INSERT-then-conflict race-free) + NHN SMS 어댑터(mock/real) + Cloudflare Queues(`solsol-dispatch`) + consumer worker(`dispatch_state` 천이) + 프로덕션 배포 #6(Version `b30dc2a3...`, Producer+Consumer 동시 바인딩) + NHN webhook 핸들러(`POST /webhooks/nhn/sms`, HMAC-SHA256 서명 검증·dedup_key 멱등·recv_state 자동 천이) + **Email · Kakao(알림톡/친구톡) · Push 3채널 동시 추가**(adapter·producer·worker·OpenAPI 4지점, channel branching generic화·EMAIL_PRICING=0.65·KAKAO_PRICING={alimtalk:8,friendtalk:12}·PUSH_PRICING=0.5) + 프로덕션 배포 #7(Version `12dae362...`). 큐 e2e + 웹훅 검증은 Cloudflare 1105 회복 후 재시도. **트랙 C** — `solsol` 사용자단 추가 작업으로 메시지 관리 랜딩페이지 만들기(목록·기본형/확장형 폼·미리보기) + 문의하기 경로를 `/account/inquiry`로 이동(GNB·푸터·사이트맵 링크 정리) + 나의 페이지 섹션(공통 셸 + 9개 라우트·회원 정보 변경·결제 카드 관리) + 크레딧 충전 플로우(시안 기반 재구성·결제 컨펌·결과 화면) Pages 배포 #44·#45·#46. **트랙 D** — `solsol-api` 루트(`/`) placeholder JSON을 `c.redirect('/doc')`로 교체해 Scalar API 문서로 302 이동, Workers 재배포(Version `f3fd3eb4...`). **트랙 E** — `solsol-api` §13 working tree WIP 정식 커밋 — **RCS 채널(5채널째)** adapter(`rcs.ts` sms/lms/mms/template 4타입)·producer·consumer·webhook + `RCS_PRICING{sms:12,lms:40,mms:120,template:50}` + **Export 잡** `TB_EXPORT_JOB` + `/export-jobs` CRUD(시안 다운로드 요청 PU 대응, 90일 윈도우 우회) + **Flow 정의** `TB_FLOW_DEFINITION/RUN/STEP_RUN` + `/flow-definitions` CRUD(nodes JSON 검증: order 연속·첫 노드 always·5채널 enum) + OpenAPI 4지점(+230) + `0002_export_flow.sql` 마이그레이션 → 단일 배치 커밋 `1e7bd61`(12 files +1228 −16) + 프로덕션 배포 #8(Version `95f9f894...`). DDL 적용은 1105 회복 후 `/admin/migrate` 경로로 보류(프런트 호출처 0개 — 무영향). |

## 작성 시점

- 큰 마일스톤 마무리 직후
- 그날 끝낼 때 짧게라도 한 줄 요약 + 산출물 목록만이라도

## 검색

특정 결정을 언제 했는지 찾을 때 `grep`:

```bash
grep -rn "Smart Placement" doc/history/
grep -rn "Aurora 노출" doc/history/
```
