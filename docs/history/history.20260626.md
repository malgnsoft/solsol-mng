# 2026-06-26 — WBS 재구성(퍼블리싱·설계·구현) + /screens 목업 링크 정상화

> **한 줄 요약** — `/screens` 목업 링크 캐시/상대경로 이슈 정상화(잔여 D1 토글 정리) 후, **WBS 스텝 4·5·6을 「퍼블리싱(목업)·설계·구현」으로 전면 재구성** — 화면영역(FR/AD/BR/BO)×큰 기능 40항목으로 신규 생성하고, 어제 3영역 목업·검증 성과를 반영. 라이브 D1 동기화.

## 1. `/screens` 목업 링크 정상화

- 증상: 화면 페이지에서 AD01·BR01 퍼블리싱 0%로 표시(미갱신). 진단 결과 **프로덕션 데이터는 정상**(API 번들에 AD01 154·BR01 44 URL), 보이던 0%는 **캐시된 옛 화면**(`useFetch` payload 캐시).
- 정리: 잔여 D1 토글 행 2건(`S-FR01-9001-001`·`9002-001`, publish=0) 삭제 → FR01 100% 복원·가려졌던 목업 링크 노출. `screen_status` 행 0.

## 2. WBS 스텝 4·5·6 재구성 (퍼블리싱·설계·구현)

사용자 지시: 스텝 4·5·6 기존 항목 전량 삭제 + 화면단위·큰 기능 중심으로 신규 생성, 어제 작업 반영.

- **스텝 라벨**: `4 퍼블리싱(목업)` · `5 설계` · `6 구현`. 스텝 1·2·3·7은 보존(라이브 편집 유지).
- **스텝 4 퍼블리싱(목업) — 13항목**: 사용자단(FR) 5 · 관리자단(AD) 4 · 브랜드(BR) 3 · 브랜드 관리자단(BO) 1. 어제 배포한 3영역 목업을 영역×기능으로 반영(목업 URL을 href로, FR/AD/BR 100%·BO 대기). 검증 결과 주석(FR blocker5·AD 모달blocker2·BR major4).
- **스텝 5 설계 — 8항목**: 화면설계·정책·검증(화면목록 v1.2·화면설계서·정책설계서·검증패키지) + 개발설계(DB·기능명세·API·외부연계).
- **스텝 6 구현 — 19항목**: 공통·기반 5 + 관리자단(AD) 7 + 사용자단(FR) 4 + 브랜드(BR) 2 + BackOffice 1. 영역×큰기능 단위로 현재 진척 반영.
- **데이터 동기화**: 라이브 D1 `wbs_item` 스텝4·5·6 DELETE→INSERT(40), 스텝1·2·3·7 보존. `wbsData.ts`(폴백)·`seed.sql`(시드) 동기 갱신. stageMeta progress 4=90·5=85·6=48.
- 라이브 `/api/wbs` 검증: 스텝별 5·7·3·13·8·19·6 정상.

## 산출물

- `app/utils/wbsData.ts`·`server/db/seed.sql` 갱신 + 라이브 D1 동기화 → <https://solsol-mng.pages.dev/wbs>
- `/screens` 목업 링크 정상화(3영역 전수, D1 잔여행 정리).

## 다음 단계

- 보완 라운드: FR blocker5(쿠폰 정률)·AD blocker2(학습자·빌더 모달)·BR major4(무료체험·유효시간) → 재검증.
- BO(브랜드 관리자단) 화면목록 확정 시 목업·구현 항목 확장.
