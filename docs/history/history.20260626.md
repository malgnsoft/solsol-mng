# 2026-06-26 — WBS 재구성(퍼블리싱·설계·구현) + /screens 목업 링크 정상화

> **한 줄 요약** — `/screens` 목업 링크 캐시/상대경로 이슈 정상화(잔여 D1 토글 정리) 후, **WBS 스텝 4·5·6을 「퍼블리싱(목업)·설계·구현」으로 전면 재구성** — 화면영역(FR/AD/BR/BO)×큰 기능 40항목으로 신규 생성하고, 어제 3영역 목업·검증 성과를 반영. 라이브 D1 동기화.

## 1. `/screens` 목업 링크 정상화

- 증상: 화면 페이지에서 AD01·BR01 퍼블리싱 0%로 표시(미갱신). 진단 결과 **프로덕션 데이터는 정상**(API 번들에 AD01 154·BR01 44 URL), 보이던 0%는 **캐시된 옛 화면**(`useFetch` payload 캐시).
- 정리: 잔여 D1 토글 행 2건(`S-FR01-9001-001`·`9002-001`, publish=0) 삭제 → FR01 100% 복원·가려졌던 목업 링크 노출. `screen_status` 행 0.

## 2. WBS 스텝 4·5·6 재구성 (퍼블리싱·설계·구현)

사용자 지시: 스텝 4·5·6 기존 항목 전량 삭제 + 화면단위·큰 기능 중심으로 신규 생성, 어제 작업 반영.

- **스텝 라벨**: `4 퍼블리싱(목업)` · `5 설계` · `6 구현`. 스텝 1·2·3·7은 보존(라이브 편집 유지).
- **스텝 4 퍼블리싱(목업) — 13항목**: 사용자단(FR) 5 · 관리자단(AD) 4 · 브랜드(BR) 3 · 브랜드 관리자단(BO) 1. 어제 배포한 3영역 목업을 영역×기능으로 반영(목업 URL을 href로, FR/AD/BR 100%·BO 대기). 검증 결과 주석(FR blocker5·AD 모달blocker2·BR major4).
- **스텝 5 설계 — 8항목**: 화면설계·정책·검증(화면목록 v1.2·화면설계서·정책설계서·검증패키지) + 개발설계(DB·기능명세·API·외부연계).
- **스텝 6 구현 — 19항목**: 공통·기반 5 + 관리자단(AD) 7 + 사용자단(FR) 4 + 브랜드(BR) 2 + BackOffice 1. 영역×큰기능 단위로 현재 진척 반영.
- **데이터 동기화**: 라이브 D1 `wbs_item` 스텝4·5·6 DELETE→INSERT(40), 스텝1·2·3·7 보존. `wbsData.ts`(폴백)·`seed.sql`(시드) 동기 갱신. stageMeta progress 4=90·5=85·6=48.
- 라이브 `/api/wbs` 검증: 스텝별 5·7·3·13·8·19·6 정상.
- **담당자 재배정**: 서만원·조수현(단독·콤보 4종) 담당 전량 **김도형**으로 변경(22항목). 이어 **스텝 4·5·6 담당자 전체를 김도형으로 통일**(40항목).
- **스텝 4·5·6 일정 재생성**: 어제 작업분(3영역 목업·화면목록·검증=14항목)은 일정 유지, 나머지(26항목)는 2026-06-26 기준 새 일정으로 재편(설계 6~8월·구현 6~9월). wbsData·seed·라이브 D1 일괄. (스키마는 start/end만 — '완료'는 완료 항목의 end로 표기.)
- **담당 칩 정리**: WBS 담당자 필터에서 서만원·조수현 제거(`PEOPLE`). **스텝6(구현) 19항목 진행률 전부 0%**로 변경(stageMeta[6]=0 · wbsData·seed·D1). **스텝5 '개발 설계' 그룹 4항목(DB·기능명세·API·외부연계)도 새로 시작 → 0%**(stageMeta[5]=48 재계산).
- **WBS 상단 떨림 수정**: 스크롤 접기 피드백 루프를 scrollLock(300ms)+canCollapse+히스테리시스로 차단(malgn-studio-mng 방식 이식).
- **책임(responsible) 컬럼 신설**: 구분·작업 ↔ 담당 사이에 '책임' 컬럼 추가. `wbs_item.responsible` 컬럼(마이그레이션 0004) + 초기값=담당(owner). 운영: **책임=책임자(실무자)** / **담당=실제 작업한 클로드 에이전트**. schema·API(get/post/patch)·페이지(헤더·셀·편집폼)·seed·라이브 D1 일괄(61행 responsible=owner).
- **책임=김도형 항목의 담당을 에이전트로 배정**(맘총괄 판단·36항목): 목업 FR/BR→frontend-developer·AD→admin-developer / 설계 DB→dba·API/외부연계→api-developer·기능명세→planner / 구현 AD→admin-developer·FR→frontend-developer(강의·상품 lms-developer)·BR→frontend-developer·공통 api-developer(인프라 deployer). 책임=김덕조 4항목(화면설계·정책·검증)은 미변경. 향후 모든 지시는 맘총괄을 통해 진행.
- **WBS 담당 에이전트명 공식 한글화**: `~/.claude/agents` 정의(dotype:identity) 반영 — 강프개·임관개·조백개·한데관·최기획·신배담·배현우.
- **Step 7 세분화**(스프레드시트 107~124·18항목) + **진척률 자동 산출 로직**(항목→스텝→전체 자동 연동, wbsStageMeta=가중치만) + 대시보드 동반 적용.
- **검증 정본(docs/validation) 갱신·배포**(`eeb671c`): `00_화면목록` v1.2(+169)·`05_정책설계서`(+462)·**`06_API계약` 신설(413줄)**·`01~04`/README 정합(총 +878/−175, 10파일). 라이브 `/validation`·`/docs/validation/06_API계약` 200.

## 산출물

- `app/utils/wbsData.ts`·`server/db/seed.sql` 갱신 + 라이브 D1 동기화 → <https://solsol-mng.pages.dev/wbs>
- `/screens` 목업 링크 정상화(3영역 전수, D1 잔여행 정리).

- **Step 7 세분화 재구성**(스프레드시트 107~124 반영·18항목): 통합 6항목 → 플랫폼 기획 3(서브페이지·템플릿12종·SEO)·테스트 2(산출물 체크·전체 QA)·운영 및 정책 5(운영가이드·이용약관·개인정보·마케팅동의 약관·FAQ)·마케팅 3(알림마케팅·섭외·프로모션)·계약 4(토스PG·NHN·펌뱅킹·나이스 분리)·WBS 1(D-F 관리). 누락 항목 보강. wbsData·seed·라이브 D1 일괄, stageMeta[7]=17.

- **진척률 자동 산출 로직 강화**: 스텝/전체 진척을 `wbsStageMeta` 하드코딩 → **항목 progress 기반 자동 산출**로 전환. 스텝 진척=항목 평균, 전체 진척=스텝(단계 가중치)×스텝진척 가중평균. 항목 progress가 바뀌면 스텝·전체가 자동 반영. 현행: 스텝 1:94·2:87·3:87·4:92·5:48·6:0·7:17 → 전체 **36.2%**.

## 다음 단계

- 보완 라운드: FR blocker5(쿠폰 정률)·AD blocker2(학습자·빌더 모달)·BR major4(무료체험·유효시간) → 재검증.
- BO(브랜드 관리자단) 화면목록 확정 시 목업·구현 항목 확장.
