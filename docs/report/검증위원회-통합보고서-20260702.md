# 쏠쏠·브랜드 검증 위원회 — 통합 종합 보고서

> **기준일**: 2026-07-02 (KST) · **주관**: 검증 위원회(총괄 대행, 위원장=qa-lead) · **최종 라운드**: committee-r01
> **정본**: `docs/validation/`(00~06·`_exports/png`, **읽기전용 무수정**) · **방법**: `docs/DEV_VALIDATION_PROCESS.md` · **원장**: `docs/dev-validation/_ledger.md`
> **성격**: 실앱·백엔드 conformance 게이트. 정본·앱 코드 **전 과정 무수정**(검증·기록만).
> **본 문서**: `docs/report/` 위원회 보고서 6종을 취합한 **단일 통합본**. 원본 6종은 `docs/report/_archive/`에 형식통일 보존(부록).

---

## 0. 총평 (Executive Summary)

**위원회 종합 판정: ❌ NO-GO** — open blocker **8건** 미해소(committee-r01 5 + r02 백엔드 신규 2 + r03 데이터모델 신규 1; 브랜드 관리자단 SEC-1은 r03-D02와 동일 계열).

- **화면 인터페이스 트랙은 성숙** — FR01 GO, AD01·BR01 커버리지 완비, 05 정책 대부분 정합. 실앱 3종은 목업 대비 성숙도가 크게 높다(공통 마스킹 유틸·RBAC 프리셋·쿠폰 정액 가드·알림 카탈로그·토스트 통일).
- **blocker는 보안·RBAC·데이터·계약·금전원장 트랙에 집중** — 운영 배포 게이트의 실질 차단 요인.
- **유일 조건부 GO**: `solsol-brand`(브랜드 사용자단) — blocker 0, 현재 배포본 유효.
- **관리 허브 `solsol-mng`**: 검증 대상 비적용(오너 결정 ②) — 배포 스모크만.

| 앱 | 종합 판정 | 차단 blocker |
|---|:--:|---|
| solsol (사용자단·FR01) | ❌ NO-GO | API-D01(주문내역 EP 부재) |
| solsol-admin (관리자단·AD01) | ❌ NO-GO | AD01-D01(RBAC 직접URL) · SEC-D02(demo-login 우회) |
| solsol-api (백엔드) | ❌ NO-GO | DAT-D01(크레딧 스키마) · API-D01 · **SEC-r02-D01(`/health/db` 무인증)** |
| **solsol-brand (사용자단·BR01)** | ✅ **GO(조건부)** | 없음 (배포본 유효) |
| solsol-brand-admin (관리자단·BA01) | ⏸ HOLD | SEC-1(크레딧 조정 멱등·원자성) |
| solsol-brand-api (백엔드) | ❌ NO-GO | **PRV-r02-D01(admin/users 셀러 PII 무마스킹)** · SEC-1 EP 노출 보류 |
| docs/data-model (SoT DDL) | ❌ NO-GO | DAT-D02(master.sql 드리프트) · **r03-D02(크레딧 멱등 uk NULL)** |

> **결함 총계**: committee-r01(7트랙) 상 5·중 17·하 18 + r02(백엔드) 상 3·중 10·하 14 + r03(데이터모델) 상 3·중 15·하 13 + 브랜드 관리자단 심화 상 1·중 5. (라운드 간 재확인·중복 존재 — open blocker **유니크 8건**.)

---

## 1. 위원회 구성 · 방법 · 라운드 이력

### 1-1. 위원회(글로벌 에이전트팀, 총괄 대행)
위원장(qa-lead) 총괄, 7개 트랙 병렬 + 앱별 3렌즈 심화.

| 트랙 | 위원(역할) | 대상 |
|---|---|---|
| T1 화면 FR01 | qa | solsol(사용자단) |
| T2 화면 AD01 | qa | solsol-admin(관리자단) |
| T3 화면 BR01 | qa | solsol-brand(브랜드 사용자단) |
| T5 보안(횡단) | security-reviewer | 5개 앱 |
| T6 개인정보(횡단) | privacy-officer | 5개 앱 |
| T7 데이터 5축 | dba | solsol-api · solsol-brand-api |
| T8 API 계약 | api-developer | solsol-api · solsol-brand-api |
| 통합·판정 | qa-lead(위원장) | 전체 |

### 1-2. 검증 축 (정본 인용)
화면 **9축**(존재·IA/라우팅·상태·공통컴포넌트·마스킹·카피[PNG]·디자인토큰·반응형·인터랙션) + **05 정책 확정 6/16건** + **데이터 5축**(스키마정합·마이그레이션안전·시드정합·데이터정합성·성능) + **보안 상시**(인증세션·RBAC·시크릿·남용) + **마스킹 3레이어** + **계약 정합**. 권위: 05 정책 > 화면설계서 00~03 > 04 요약 > 참조 PNG > 스타일가이드(`[추정]` OCR 비권위).

### 1-3. 라운드 이력 (본 통합본이 합친 6개 원본)
| 라운드/문서 | 성격 | blocker | 상태 |
|---|---|---|---|
| **R-VC1** (`…-보고서`) | 3앱 화면 초기 종합 | 4 (X-1 demo·X-2 owner비번·RBAC·M-1) | 초기 → committee-r01로 진화 |
| **3앱 종합** (`…-종합보고서`) | R-VC1 재정리(B-1~B-4) | 4 | 초기 |
| **committee-r01** (`committee-summary`) | 7트랙 종합·원장 연계 | 5 | 종합 골격 |
| **committee-r02** (`…-r02-backend`) | 백엔드 계약·데이터 심화(api·brand-api) | +2 신규(SEC-r02-D01·PRV-r02-D01) | 백엔드 심화 |
| **committee-r03** (`…-datamodel-r03`) | 데이터모델 Round3(대규모 개편 재검) | +1 신규(r03-D02) + 재확인 2 | 데이터 심화 |
| **FR01 심화** | 사용자단 3렌즈 | 0(상)·조건부 | 심화 |
| **AD01 심화** | 관리자단 3렌즈 | 2(S-1·S-2) | 심화 |
| **브랜드 관리자단 심화** | brand-admin+brand-api 3렌즈(신 5EP 포함) | 1(SEC-1) | 심화 |

> **blocker 명칭 정합**: 초기 `X-1/B-3`(demo-login) = 심화 `S-1` = 최종 **SEC-cttee-r01-D02**. 초기 `B-2`(RBAC) = 최종 **AD01-cttee-r01-D01**. 초기 `X-2/B-4/S-2`(owner 실비번 git 커밋 `ce11a5d`)는 최종 5건에 별도 등재되진 않았으나 **critical 잔존**으로 §8에 유지. 초기 `B-1`(BR01 M-1 무료체험 4곳)은 committee-r01 BR01 재판정에서 재플래그되지 않음 → **해소 추정, §7에서 확인 대상**.

---

## 2. Blocker(상) 통합 — 6건 · 해제조건

| # | 결함ID | 앱/소유 | 요약 | 해제조건 | 담당 |
|---|---|---|---|---|---|
| 1 | **AD01-cttee-r01-D01** | solsol-admin | **RBAC 직접 URL 미차단** — 미들웨어가 `isAuthenticated`만 검사, `hasMenu` 미검사 → 강사/서브강사가 차단메뉴 직접 URL 진입 시 셸 렌더(EXC-06 부재·05 M-2 규칙5 위반). *데이터는 solsol-api 서버 roleGuard가 최종 차단하나, 셸 노출 자체가 게이트 위반.* | `auth.global.ts` 메뉴권한 게이트(prefix→menuKey·false→restricted)+역할별 세션 재검 | admin-developer |
| 2 | **SEC-cttee-r01-D02** | solsol-admin | **demo-login fail-open** — `previewDemo` 미설정 시 엔드포인트 활성, 익명 same-origin POST가 seed superadmin **`role:owner` 세션 원클릭 발급**(라이브 200 실증·default-open) | default-deny 반전(명시 enable + `APP_ENV!=='production'` 이중가드)+prod `previewDemo=false` 검증 | admin-developer + deployer |
| 3 | **DAT-cttee-r01-D01** | solsol-api | **크레딧 스키마 이탈** — ORM/라우트는 3테이블(ACCOUNT/CHARGE/LEDGER), 마이그레이션은 단일 `TB_CREDIT` → `/api/admin/credits` 런타임 실패 위험 | 단일 `TB_CREDIT` 재정렬 or 3테이블 정본승격(ERD·master.sql·brand-api·마이그 **4자 동시**). **결정 전 배포금지** | dba |
| 4 | **DAT-cttee-r01-D02** | docs/data-model(SoT) | **master.sql 사본 드리프트** — `TB_CONTACT`/`REPLY`/`NEWS` 누락(13 ↔ ERD·구현 16) | master.sql 16테이블 현행화+카운트 주석 정정(즉시 교정 가능) | dba |
| 5 | **API-cttee-r01-D01** | solsol + solsol-api | **`GET /api/my-orders` 라우트 부재** → 항상 404 → mock 폴백이 주문내역 실데이터 행복경로 영구 차단 | 프론트 `useOrder.ts`를 `GET /api/orders`로 정렬(권장) or API 별칭 추가+재검 | frontend/api-developer |
| 6 | **SEC-1 (brand)** ≈ **r03-D02** | solsol-brand-api / brand-admin | **크레딧 조정 EP 멱등·원자성 결함**(금전 원장) — ① 멱등 가드 SELECT→INSERT 비원자(Hyperdrive stale) → 이중 조정 ② `uk(site_id,idempotency_key,source_credit_id)` 부재/**NULL-distinct** → 증가행(charge) `source_credit_id=NULL`이라 동일 키도 이중 지급 차단 불가 ③ 차감이 트랜잭션·행잠금 밖 → lot 이중소진·음수 잔액 | 멱등 `uk(site_id,idempotency_key)`(NULL 미포함) INSERT-first or `COALESCE(source_credit_id,0)` 생성컬럼 uk + 잔액행 `SELECT…FOR UPDATE` **단일 트랜잭션+행잠금**. **mng master.sql + api 000_master 동시 개정**. 재검증 전 EP 운영 노출 보류 | api-developer + dba |
| 7 | **SEC-cttee-r02-D01** (신규) | solsol-api | **`/health/db`·`/health/db/grants` 프로덕션 무인증 공개** → 전체 테넌트 스키마명·`SHOW GRANTS`·DB버전 노출 | brand-api `index.ts` 게이트 이식(prod `X-Ops-Secret===OPS_SECRET`, grants prod 무조건 차단) + 재검 | api-developer |
| 8 | **PRV-cttee-r02-D01** (신규) | solsol-brand-api | **`/admin/users` 셀러 PII 무마스킹** — 목록/상세가 loginId·email·name·phone 무마스킹 노출(mask 함수 dead code)·C-6 타인조회 위반 | `maskName/Email/Phone` 적용 후 재검, or 관리자 열람목적 오너 정책확정 시 원장 사유·승인자 기재(강등) | admin/api-developer |

---

## 3. 트랙/렌즈별 판정 (committee-r01)

| 트랙 | 상/중/하 | 판정 | 핵심 |
|---|:--:|:--:|---|
| T1 FR01 화면 | 0/1/1 | **GO** | 커버리지 100%(42+모달14), 05 6건 위반 0, build EXIT 0. 중=본인화면 휴대폰 과마스킹(C-6) |
| T2 AD01 화면 | 1/0/2 | **NO-GO** | blocker=RBAC 직접 URL 미차단(EXC-06 부재) |
| T3 BR01 화면 | 0/2/3 | **GO(조건부)** | 중=비번 C-3 4종강제(정책 3종)·C01 약관모달 미구현 |
| T5 보안(횡단) | 1/6/3 | **CONDITIONAL** | blocker=solsol-admin demo-login fail-open |
| T6 개인정보(횡단) | 0/3/2 | **조건부 통과** | 중=웹훅 raw_payload 마스킹·정통망법 야간차단·국외이전(실전송 시 필수) |
| T7 데이터 5축 | 2/5/2 | **NO-GO** | blocker=크레딧 스키마 이탈·master.sql 정본 지연 |
| T8 API 계약 | 1/0/5 | **NO-GO(조건부)** | blocker=solsol `/api/my-orders` 라우트 부재 |

---

## 4. 앱별 상세 (심화 라운드 반영)

### 4-1. solsol (사용자단·FR01) — 조건부 GO / go-live 잔여
- **QA GO**: 커버리지 본화면 42/42·모달 14/14=100%, 9축 정합(상태축 🔺 런타임 미실행), 05 6건 위반 0(쿠폰 정액·C-6 마스킹 정합), `typecheck EXIT 0`. 하 3(auth 토스트 프리셋 우회·실연동 스텁 정직표기·중복 import).
- **보안 조건부**: CORS 화이트리스트·JWT 회전+재사용탐지·IDOR 방어·시크릿 무노출 견고(critical 0). **운영 전 필수**: **SEC-A(상·구조·오너)** refresh 쿠키 `pages.dev↔workers.dev` 서드파티(SameSite=None)로 세션 갱신 단절 위험 · **SEC-C(중)** `/health/db*` 무인증 DB 토폴로지·GRANT 노출. 후속(비차단): access 토큰 localStorage(XSS 15분)·레이트리밋 미구현.
- **개인정보 GO**: 카드 뒤4·본인화면 비마스킹·비번/인증코드 미출력·console PII 0·비가역 게이트(탈퇴 이중컨펌) 정합. **중**: 마케팅 동의 시점 저장(정통망법) 백엔드 계약 반영 권고.
- **차단 blocker**: API-D01(주문내역 EP). 상세 `docs/dev-validation/committee-fr01-{qa,security,privacy}.md`.

### 4-2. solsol-admin (관리자단·AD01) — NO-GO(프로덕션)
- **화면·개인정보 게이트 통과**: 커버리지 102/102·05 6건 전건 ⭕·build EXIT 0. 9축 상태🔺(API 실패 무음 목폴백)·마스킹🔺(닉네임 payload)·반응형🔺(390 미대응).
- **보안 NO-GO(프로덕션)**: **S-1 demo-login 익명 owner 세션**(=SEC-D02) · **S-2 seed owner 실비번 소스 하드코딩+공개 git 커밋(`ce11a5d`)**(→ env 이관·**비번 로테이트** 필수) · **AD01-D01 RBAC 직접 URL**. *현행 dev 공개 프리뷰는 조건부 허용, 실 고객 프로덕션 전환은 S-1·S-2 해소 전 NO-GO.*
- **양호**: 서버 roleGuard 3단 최종강제(requireAuth→requireUserType('staff')→roleGuard·EXC-06 403)·SSRF 하드닝·httpOnly 세션·비가역 컨펌.
- **중**: H-1 닉네임 payload 노출(SoT 3방향 불일치)·S-3 보안 헤더 부재·D01 반응형·D02 무음 목폴백. 상세 `AD01` 심화 원본.

### 4-3. solsol-brand (브랜드 사용자단·BR01) — ✅ GO(조건부)
- **blocker 0** — 유일 조건부 GO, 현재 배포본 유효. 커버리지 36(검증제외 3 제외)·05 대부분 정합.
- **중**: 비번 C-3 4종 강제(정책 3종)·C-2 인증코드 3분(확정 10분)·C01 약관 상세 모달 미구현. **오너/기획 판정**: color-contrast·모바일 반응형(1920 고정)·C01 모달 신설 vs SoT 개정.
- **운영 전 필수**: **[TEST-ONLY] 인증번호 완화 원복**·처리방침 개인정보보호책임자 placeholder(개인정보보호법 §30 필수기재).
- **참고**: 초기 라운드 B-1(M-1 "14일 무료체험" 4곳)은 committee-r01 재판정 미플래그 → §7 확인 대상.

### 4-4. solsol-api (백엔드) — NO-GO
- **차단**: DAT-D01(크레딧 3테이블↔단일 이탈)·API-D01(my-orders). 데이터 5축 상 2·중 5·하 2.
- **기존 보안(라운드1)**: F-1 CORS(§24 해소)·F-2 계정열거·F-3 레이트리밋·F-4 `/health/db` 정보노출·F-5 공개경로 401·F-6 `SECRET_ENC_KEY`·F-7 비밀댓글. 상세 `solsol-api-review-round1.md`.

### 4-5. solsol-brand-admin (관리자단·BA01) — ⏸ HOLD
- **커버리지 27/27**·두 레포 typecheck EXIT 0·05 M-1·C-5 준수·마스킹 5축 회귀 0.
- **차단 SEC-1**(§2 #6): 신설 5EP 중 **크레딧 조정(금전 원장) 멱등·원자성** → 운영 노출 보류.
- **중**: SEC-2 CS 뮤테이션 requireRole 부재(support JWT 직접호출)·QA-D1 원장 pageSize override 미전달(20건 초과 누락)·QA-D2 CS 상세 실연동 시 이메일 날조·QA-D3 공지목록 published 필터 count 불일치.
- **실전환 잠금**: `NUXT_API_BASE` 미설정 + brand-api 운영자 시드 대기.

### 4-6. solsol-brand-api (백엔드) — ⚠ CONDITIONAL
- blocker 0(실결제 시 SEC-D09 승격). SEC-1 EP는 노출 보류.
- 잔여: SEC-3 refresh 무상태 원장 부재(`TB_SESSION` 도입)·운영자 시드 미적용·TOSS 미주입(staging-gate)·CF WAF·`ALLOWED_ORIGINS`·R2 미바인딩·토스 웹훅 서명검증 완성.

---

## 5. 05 확정 정합 · 커버리지 (plan-lead 간사)

- **커버리지**: FR01 48 · AD01 107 · BR01 36(검증제외 3) — **핵심 누락 0**, 3영역 사실상 완비. 유령(경미): FR `index`·`callback`, BR `pricing-light`.
- **검증제외 준수**: 폐기(마이페이지>설정>알림)·외부IdP·BR 프로덕트/데모/주요기능 **오존재 0건 ⭕**.
- **05 정합**: C-1 닉네임·C-3 비번·C-4 쿠폰정액·C-6 마스킹(FR 휴대폰 1건 예외)·M-4 알림 10종·M-3 단가Open ⭕ / M-2 RBAC(런타임 미강제=AD01-D01)·C-2 인증시간(BR 3분=위반)·C-5 토스트(단계적용) 🔺 / **M-1 무료체험(초기 BR 4곳=B-1, 최종 재확인 대상)**.

---

## 6. 중(major) 결함 통합

- **FR01**: 휴대폰 과마스킹(C-6)·마케팅 토글 키보드 불가·마케팅 동의 시점 미저장.
- **AD01**: H-1 닉네임 payload 노출·S-3 보안헤더·D01 반응형·D02 무음 목폴백·reset-password brandName 빈렌더·역할탭 죽은 컨트롤(role 미전송).
- **BR01**: C-2 인증 3분·C-3 4종강제·GNB 페이지별 불일치·Footer 24 누락·반응형 부재·가입 약관 버전/동의일시 미기록·만14세 게이트 앱간 불일치.
- **브랜드 관리자단**: SEC-2 CS requireRole·QA-D1 원장 페이지네이션·QA-D2 이메일 날조·QA-D3 공지 count 불일치·SEC-3 refresh 원장.
- **개인정보(횡단)**: 웹훅 raw_payload 마스킹·정통망법 야간차단 실효·국외이전(GA/Meta) — 전부 실전송 시 필수.

---

## 7. SoT 내부 충돌 (기획/오너 확정 — 검증정본 밖 처리)
1. **인증메일 유효시간**: `00_화면목록:354` "1시간" ↔ `05 C-2` "재설정 30분" 상충.
2. **서브강사 운영 권한**: `05 M-2` 표(운영=△·허용) ↔ 핵심규칙#3(운영 제외)·`06 §2.8`(차단) 모순 → RBAC 프리셋 기준선 불안정.
3. **K-1 관리자 타인조회 마스킹 범위**: 이름·이메일 노출 범위 미확정(현재 과보수적).
4. **닉네임 표시식별자 예외**(H-1): 06 §0(마스킹) vs 04 CMP-01(열거 없음) vs 백엔드(노출) 3방향.
5. **BR01 M-1 무료체험**: 초기 4곳 잔존 → **현행 실앱 재확인 필요**(해소 여부).
6. **자막 편집 화면 유효성**·**폴더 삭제 정책**(위캔디오 위임·미분류 이동 vs 삭제).

---

## 8. Critical 잔존 (5건 밖·반드시 처리)
- **owner 실비번 git 커밋(`ce11a5d`, X-2/S-2)** — server-only라 클라 미유출이나 공개 소스 노출 자체가 취약. B-3/SEC-D02(demo-login)와 결합 시 **익명 원격 owner 장악** → 소스 제거·env-only·**비번 로테이트** 필수(미리보기 여부 무관 즉시).

---

## 9. 이월(비-blocker) — 결함 아님, 오너/기획/실연동 대기
| 항목 | 성격 | 소유 | 시점 |
|---|---|---|---|
| [TEST-ONLY] 인증번호 완화(§21) | 테스트 의도 | solsol-brand | 운영 전 원복 필수 |
| color-contrast / 모바일 반응형(1920 고정) / C01 약관모달 | 오너·디자인/기획 판정 | solsol-brand | 오너 판정 |
| staging-gate(결제/구독/크레딧·웹훅) | 설계 게이트 | solsol-api·brand-api | 실 PG 연동 시(SEC-D09 승격) |
| brand 운영자 시드 미적용(DAT-D07) | 의도된 placeholder | solsol-brand-api | DBA 자기 env 적용 |
| SEC D01/D03~D06 · PRV D01~D05 | 실연동 전 필수 | 각 API | stage8 실연동 전(security·privacy 서명) |

---

## 10. 시정 우선순위 · 담당 (다음 라운드)
1. **DAT-D02**(즉시 교정) — master.sql 16테이블 현행화(저위험 즉시 close). [dba]
2. **DAT-D01**(결정 선행) — 크레딧 단일/3테이블 모델 확정 → 4자 재정렬(결제 인접·런타임 위험, 최우선 결정). [dba]
3. **API-D01** — 프론트 `useOrder`→`/api/orders` 정렬(소규모). [frontend/api]
4. **SEC-D02 + AD01-D01**(solsol-admin 동시) — demo-login default-deny + RBAC 미들웨어 게이트 + 역할별 세션 재검. [admin-developer]
5. **Critical: owner 비번 로테이트·env 이관**(§8). [admin-developer + deployer/dba]
6. **SEC-1**(브랜드) — 크레딧 조정 원자 멱등+행잠금 트랜잭션 → 재검증 → GO 시 EP 노출. [api-developer + dba]
7. 보완 라운드 재검증(6건 close → open blocker 0 검산) → **실연동 라운드(stage8)** SEC/PRV 서명.

---

## 11. 이월원장 연계 (`docs/dev-validation/_ledger.md`)
- **기존 open 3건 재판정**: `FR01-r01-B*`→**closed**(실앱 FR01 GO·정률표기 0) / `AD01-mock-r02-B*`→**이관**(목업→실앱·AD01-D01 승계) / `BR01-v5-r01-B*`→**won't-fix**(오너 A형).
- **신규 open(유니크 8)**: r01 5건(AD01-D01·SEC-D02·DAT-D01·DAT-D02·API-D01) + r02 2건(SEC-r02-D01·PRV-r02-D01) + r03 1건(data-model-r03-D02 ≈ 브랜드 SEC-1).
- **검산: open blocker = 8 → 게이트 종료조건 "blocker 0" 미충족(NO-GO).** (원장 반영: r02·r03 신규 3건 등재 권고.)
- **근본원인(r03) — 정본 표류(4자 비동기)**: 데이터 정본이 mng `data-model`(13/91) ↔ `solsol-api`+`ERD.md`(16/92) ↔ ORM `schema.master.ts`(구 크레딧 3분할)로 분기. **선결정(오너/architect)**: "어느 사본이 정본인가 + 브랜드 테이블이 크리에이터 LMS master 스코프인가" 단일화 → DDL=ERD=README=ORM 4자 동기 시 다수 중결함 일괄 해소.

---

## 12. 방법 · 한계 (증거 우선 고지)
- 정적 검증: FR01 `typecheck EXIT 0`, AD01/브랜드 `build EXIT 0`. AD01 `vue-tsc` 미설치로 타입레벨 미검증(198~211건 baseline·별도 게이트 권장).
- **브라우저 런타임 전수 스모크·PNG 픽셀 대조는 미실행**(코드 바인딩·일부 라이브 프로브 기반) → 실연동 라운드에서 별도 수행 권고.
- 백엔드 F-1~F-7은 `solsol-api-review-round1.md` 참조. 운영 DB 접근·real-mode 동작은 코드 근거 추론.
- **검증 정본·앱 코드 무수정.** 실 PII/시크릿 값은 위치·유형만 표기(미기재).

---

## 13. 참여 위원 활동내역
| 트랙 | 위원(역할) | 수행 | 산출 |
|---|---|---|---|
| 실행계획·통합 | qa-lead(위원장) | 축·트랙·프로파일·종료조건 설계·7트랙 통합·앱별 판정·원장 집계 | 실행계획 + 통합 판정 + `_ledger` 갱신 |
| T1 FR01 | qa | 커버리지 42+14·9축·05 6건·typecheck | `committee-fr01-qa.md` (GO) |
| T2 AD01 | qa | 107 커버리지·9축·RBAC 노출·build·vue-tsc | `AD01-committee-r01.md` (NO-GO) |
| T3 BR01 | qa | 36 커버리지·9축·M-10·C-3·build | `BR01-committee-r01.md` (GO조건부) |
| 보안 | security-reviewer | 5앱 인증세션·RBAC·시크릿·남용·demo-login 라이브 프로브·SEC-1 금전원장 | `security-committee-r01.md` (CONDITIONAL) |
| 개인정보 | privacy-officer | 마스킹 3레이어·동의·비가역·정통망법 | `privacy-committee-r01.md` (조건부) |
| 데이터 5축 | dba | 스키마 4자 동기화·마이그·시드·정합·성능 | `data-committee-r01.md` (NO-GO) |
| API 계약 | api-developer | EP·엔벨로프·프론트 계약 매칭 | `api-contract-committee-r01.md` (NO-GO조건부) |
| 간사(정합) | plan-lead | 272 화면ID 커버리지·05 16건·검증제외 준수 | 커버리지 매트릭스 |

---

## 부록 — 원본 문서 경로
- **원본 위원회 보고서 8종**(형식통일 보존): `docs/report/_archive/검증위원회-20260702-*.md` (인덱스 `_archive/README.md`).
- **위원별 상세 결함표**: `docs/dev-validation/{FR01,AD01,BR01,security,privacy,data,api-contract}-committee-r01.md` · `committee-fr01-{qa,security,privacy}.md` · `solsol-api-committee-r02.md` · `brand-api-committee-r02.md` · `data-model-vs-validation-round3.md`.
- **이월원장**: `docs/dev-validation/_ledger.md`.
- **미결 정리(별도)**: `docs/report/쏠쏠-미결취합-20260702.md` · `브랜드-미결취합-20260702.md`.
- **정본(읽기전용)**: `docs/validation/00~06` + `_exports/png/`.
