# 쏠쏠(크리에이터 LMS) — 미결·막힌 사항 취합

> **작성**: 2026-07-02 (KST) · 총괄(chief)
> **대상**: 쏠쏠 크리에이터 LMS 3영역 — **사용자단(`solsol`/FR01) · 관리자단(`solsol-admin`/AD01) · 백엔드(`solsol-api`)**
> **출처**: `docs/report/_archive/` 원본 8종 취합 + `docs/history/history.20260701.md` §1~§24 + 검증 `docs/dev-validation/`
> **성격**: 미결(후속 개발)·막힌(오너/담당자 액션 대기)만. 완료 항목은 이력 참조.
> **범례**: 🔴 오너 결정 · 🟠 보고됨·조치 보류 · 🟡 담당자 실행/소스관리 대기 · 🟢 후속 개발 · 🔵 참고

---

## ⭐ 가장 먼저 풀 것 3 (쏠쏠)

1. **solsol-api 로그인 관문** — Blocker A(role 매핑 버그)·B(관리자 시드 부재) + F-1(CORS). **이게 막혀 관리자단 인증·쓰기 실연동 불가**.
2. **PG(TOSS) staging-gate 해제** — 사용자단·관리자단 결제·환불·정산·구독·크레딧 실동작 공통 게이트(CMP-06 비가역 → 오너 승인).
3. **수정 지시서 K-1(마스킹)·K-4(브랜드 색상)** — 목업·실앱 양쪽에 걸린 상위 결정.

---

## 🔗 공통 블로커 (쏠쏠 전 영역)

| 블로커 | 영향 | 해제 조건 / 소유 |
|---|---|---|
| **결제·정산·구독·크레딧·발송 = staging-gate 실행 차단** (`// TODO(staging-gate)`, 실 PG/TOSS 호출 0) | solsol-api → 사용자단·관리자단 전부 | 🔴 **오너 확인 후** TOSS 실키 주입·게이트 해제(비가역 CMP-06) |
| **실 DB 직결 불가** — Aurora가 Cloudflare Tunnel 뒤(3306 닫힘), `.dev.vars`에 DB 자격증명 없음 | DBA 로컬 스키마 조사·마이그레이션 불가(라이브는 `wrangler dev --remote`/`/ops/*` 경유만) | 🟡 한데관/신배담 |
| **DB 유저 `solsol` 권한 경계** — 신규 테이블은 GRANT 없으면 SELECT denied | master 테이블 추가 시 GRANT/마이그레이션 선행 | 🟡 한데관 |
| **refresh 쿠키 크로스도메인** — `.pages.dev ↔ .workers.dev` 서드파티 쿠키(Safari ITP·Chrome) | 세션 갱신 실패 위험 | 🔴 동일오리진 프록시(Pages Functions)/커스텀 도메인 정렬 |
| **소셜 OAuth 비활성** — `TB_OAUTH_CONFIG` 행 미시드 + 5종 OAuth 키(OQ-OAUTH) 미주입 | 소셜 로그인 불가 | 🟡 시드 + 🔴 키 주입 |
| **biz-legal** — 국외이전·고유식별정보 수집근거 미확정 | 컴플라이언스 | 🔴 오너/법무 |

---

## 1. 쏠쏠 사용자단 (`solsol` / FR01)

> **상태**: go-live 완료 — https://solsol.pages.dev ↔ 실 Aurora `dev` 테넌트 (조회·인증·쓰기·결제 승인대기). `solsol` `f56a8b1`(malgn). 아래는 잔여.

### 🔴 오너 확인 필요 (비가역·비용·비즈니스)
- **PG staging-gate 해제** — 결제·구독이 `{pending:true}` 승인대기까지만. 실결제·실환불은 비가역(CMP-06). → 토스 웹훅·staging 선검증 후 해제 승인.
- **R2 버킷 생성** — 첨부·이미지·아바타 업로드가 스텁(`url:null`/501), `R2_PUBLIC_BASE` 미설정. → 버킷·공개 도메인 생성 승인(비용·외부 리소스).
- **실 테넌트 프로비저닝** — 현재 `dev` 데모 테넌트 1개뿐. → 커스터머별 사이트·도메인 매핑(도메인 매핑 시 host 기반 테넌트 해석 자동).

### 🟢 기술 미결 (오너 불필요 · 즉시 착수 가능)
- **남은 조회 실연동** — 강의실·대시보드·라이브·수료증·내상품·FAQ가 "스키마 미확정"으로 **mock 폴백 유지**. 백엔드 엔드포인트(`/api/classrooms/:id`·`/dashboard`·`/api/live-rooms/:id`·`/api/me/certificates`·`/api/my-products`·`/api/faqs`)는 실재 → **응답 필드 매핑만** 하면 실 API 전환.
- 강의실·수료증 스키마 확정 후 잔여 조회 결선.

### 🟡 후속 (경/중)
- **localStorage access 토큰** — XSS 시 탈취(중). 단명(15분)이나 메모리 보관/CSP 강화 후속.
- **가입 멱등 UNIQUE 제약** — Hyperdrive stale-read 중복가입은 앱레벨(재조회·INSERT 멱등)로 완화 완료. 전용 UNIQUE 제약/락은 후속.
- **orders 스냅샷 컬럼** — 내주문은 `TB_ORDER_ITEM`↔`TB_PRODUCT` 조인 폴백으로 해소. `product_name` 스냅샷 채움 후속.

### 🟡 미커밋/미배포 · 목업 잔여
- **인증 프론트** — 커밋·푸시만·초기 배포 보류였음(§2) → 이후 §24 전체 결선·배포로 상당부 진행. **§6 회원모델 변경으로 인증 코드 재작성 필요**.
- **사용자단 목업 FR-1~14 (2026-07-02 세션)** — 로컬 수정·`pnpm build` GREEN·qa 게이트 16/16 GO, **커밋/배포 미실행**(요청 대기). 정본 `docs/dev-validation/mockup-fr01-round1.md`.
- **목업 이번 세션 남긴 것** — COM-1 모바일 반응형(오너 결정으로 **다음 전용 패스**·공수 큼) · COM-3 브랜드 색상(**K-4 확정 대기**·이름 monzo→쏠쏠만 교체) · payment-restricted 정책 방향(결제유도 vs 차단)·2-a/2-b 구독 컨펌 모달(FR 범위 밖 미착수).

---

## 2. 쏠쏠 관리자단 (`solsol-admin` / AD01)

> **상태**: 배포 차단 blocker 0 — 조회 전 도메인 + 게시판 쓰기 라이브. https://solsol-admin.pages.dev. 아래는 확장·보강·결정 대기.

### 🟥 A. 프론트 미완 (admin-developer 이어서 가능)
- **A1 쓰기 CRUD가 게시판만 연동** — 상품(생성/수정/삭제/상태변경)·사용자(초대/상태변경/권한)·콘텐츠·마케팅·사이트·설정 저장·팝업·공지·문의 답변 미연동. (쓰기 BFF·낙관적갱신 패턴 확립 → 도메인별 배선만 남음.)
- **A2 상세(detail) 실 전환 미완** — 유형별 부속(라이브 일정·화상 정원·구독자·다운로드 제한)·주문/쿠폰 상세 조인. (목록만 실 전환, 상세는 목.)
- **A3 목록 필터/정렬/페이지네이션 클라 처리** — 서버 위임 미완(대량 데이터 시 서버 쿼리 필요).

### 🟨 C. shape 불일치 → 목 유지 (백엔드 보강/합의 대기)
- 멤버십(등급 tier 구조) · settings/notification(라우트 카탈로그 vs 매트릭스 UI) · site/meta(키 불일치 `privacy_*`↔`naver/google`) · 통계 비-KPI(유형비중·평점분포·상위상품) · 게시판 게시글 탭 · 이중마스킹(하·무해, 실/목 출처 플래그 필요).

### 🔴 D. 관리자단 목업 「수정 지시서」 K-1~K-5 (선행 컨펌)
| K | 결정사항 | 대기 수정 |
|---|---|---|
| **K-1** | 관리자 타인조회(학습자/강사) 휴대폰 **마스킹 정책** | AD-4 |
| **K-2** | 관리자단 반응형 범위(전 화면 vs 우선화면+"PC권장") | COM-1 |
| **K-3** | 화면ID SoT 단일화(`00_화면목록` 단일 + `screenList.ts` 딥링크) | COM-2 |
| **K-4** | 브랜드 색상 확정(파랑/맑은소프트 vs 쏠쏠 인디고 `#6366f1`) | AD-5/COM-3 |
| (K-5) | 이메일변경 인증코드 표준값(10분 권고) | BR-2 |

### 🔵 관리자단 목업 자체 (검증 라운드2)
- **검증 라운드2 10/10 NO-GO** (정책 충실도 낮은 초기 축약본). 근본원인 8계열(마스킹유틸·RBAC·비가역게이트·인증정책·정책정합·핵심요소·정통망법·공통상태). 정본 `docs/dev-validation/AD01-mockup-round2.md`.
- 반영: **AD-1/2/3(무게이트)만** 배포(solsol-admin-mockup.pages.dev). AD-4/AD-5/COM-1/COM-2는 K-1~K-4 대기.
- **P0 공통 인프라**(마스킹 유틸·비가역 컨펌 모달·RBAC 골격·공통 상태 컴포넌트) 착수 시 다수 blocker 동시 해소.

### 🟡 미커밋 · 운영 위생 · 부채
- **미커밋** — 루트앱 BFF WIP(`app/composables/useApi.ts`·`app/pages/auth/login.vue`·`nuxt.config.ts`·`?? server/`).
- **운영 위생** — read-after-write ~60s 지연(Hyperdrive 캐시, 낙관적 갱신으로 완화) · 데모 종료 시 `NUXT_PREVIEW_DEMO=false` · `SEED_ADMIN_PASSWORD` Pages secret화·로테이션 · `devLoginEmail` public prefill 정리 · **공개 원클릭 데모 = 실 owner 접근**(노출 범위 정책·필요 시 Cloudflare Access).
- **기술 부채** — `vue-tsc` 미설치(`typeCheck:false`) → 타입 게이트 미커버, baseline 타입오류 ~211건(공용 `AppDataTable` `Record<string,unknown>` 계약 부채, 별도 리팩터 트랙).
- **기타(하)** — TOSS 결제 시크릿·Phase1 미매핑 도메인 어댑터(`DEPLOY.md` TODO) · 페이지빌더 실 DnD · 이미지 업로드/크롭 · toss 카드등록 · 카카오 연결.

---

## 3. 쏠쏠 백엔드 (`solsol-api`)

> **상태**: git 초기화 `92ad1bd` → 워커 재배포. https://solsol-api.malgnsoft.workers.dev. 정본 리뷰 `docs/dev-validation/solsol-api-review-round1.md`.

### 🟠 로그인 관문 Blocker (관리자단 실연동 불가 — 조회형은 demo-login 우회)
| ID | 내용 | 위치 |
|---|---|---|
| **Blocker A** | 로그인 role 매핑 버그(`role` vs `user_type='staff'` 직접 대조 → 항상 ROLE_MISMATCH, `TB_ROLE` 파생 필요) | `src/routes/auth.ts:895~902` |
| **Blocker B** | 관리자 계정 시드 부재(staff `TB_USER`/`TB_ROLE`/`TB_ADMIN_PERMISSION` 매트릭스) → dba 시드 마이그레이션 | `src/routes/ops.ts` |

### 🟠 보안 (라운드1 F-1~F-7) · 🟡 실행 대기
- **F-1 (라이브·즉시 핫픽스 권장)** — CORS 임의 Origin 반사 + `credentials:true`(CSRF/자격증명). `src/index.ts:59-65`. *(§24에서 `solsol.pages.dev` 추가·구버전 임의오리진 취약점 해소 — 리뷰 기준 잔여 확인 필요.)*
- **F-2** 로그인 계정열거(UNREGISTERED vs MISMATCH 통일) · **F-3** 레이트리밋/브루트포스 방어 전무(`/auth/*`, WAF/엣지) · **F-4** `/health/db`·grants 무인증 정보노출(DB버전·스키마·GRANT) · **F-5** 공개 FAQ/게시판 401 회귀(`learning.use('*')` 마운트) · **F-6** `SECRET_ENC_KEY` 미설정 시 OAuth client_secret 평문 · **F-7** 비밀댓글 `isSecret` no-op(TB 컬럼 부재).

### 🟡 계약·필드 (관리자단 실연동 관점 B1~B13)
- B1 게시판 중복 code → 500(409 alreadyExists여야, 중) · B2 zValidator 에러 봉투 비표준(중) · B7 `/health/db` verdict 오탐(`solsol_master` 네이밍, 하) · B8 경로 계약 `/api/auth/login` ↔ 실제 `/auth/login`(하) · B11 쓰기 `roleGuard` 커버리지·쓰기 레이트리밋 실제 적용 확인(중) · B12 목록 API 필드 미반환(postCount·isVisible·menuLinked·유형별 부속, 중) · B13 `TB_BOARD` 글쓰기권한/비밀정책 컬럼 부재(하).

### 🟡 staging-gate (담당자 자기 env·시크릿 필요)
- **결제·정산·구독·크레딧·발송 = staging-gate 실행 차단** — 실 PG(토스)·발송 게이트웨이 연동 대기. TOSS 시크릿(`TOSS_SECRET_KEY`·웹훅) 미주입.

### 🟡 스키마·인프라
- **앱계층 ↔ 새 스키마 불일치** — `TB_SITE`·`TB_USER` 통합·`login_id`·비밀번호 인라인·`TB_CREDIT` `_cr`/FIFO·`TB_SITE_USER` 미반영, `schema.master.ts` drizzle stale, 크레딧 FIFO 차감/멱등 미구현. *(이후 결선 상당부 진행 — 리뷰 시점 기록.)*
- **dev `/ops` 403** — `APP_ENV=production` 안전가드. `--var APP_ENV:local` 우회 중, config 정리 미결.
- **DB time_zone 근본 미적용** — Hyperdrive 멀티플렉싱으로 세션 `SET time_zone` 유지 불가 → 컬럼 `TIMESTAMP`(내부 UTC) 전환 우회(서버 UTC는 공용 dev 클러스터라 보류).
- **추가 테넌트 자동 프로비저닝** — 앱 유저에 `CREATE DATABASE` 권한 없음 → 수동만, 전용 프로비저닝 계정 필요(B-7 prod OQ).
- **크레딧 단가(`unit_price`) 값 미정**(M-3, 모델은 종량제 확정) · **기존 크레딧 백필**(운영 데이터 발생 시 구 `TB_CREDIT_CHARGE`→`TB_CREDIT` 이전).
- **라이브 채팅** 인메모리 링버퍼(Workers 격리) → 채팅 테이블 신설 · **이메일 발송 미연동**(`// TODO(mail)`) · **R2 미바인딩**(첨부·수료증 PDF).

### 🟢 SoT 소유자 확정 (에스컬레이션 — 기획/DBA)
1. **강사 `operation`(운영) 권한 불일치** — 06 §2.8(차단) vs 05 M-2 표(허용). 코드는 상위권위 05를 따름 → 06 정렬 필요.
2. **자막 편집 화면(S-AD01-0301-005) 유효성** — 자막 테이블 삭제(위캔디오 위임)와 상충 → 위캔디오 프록시 재정의 vs 제거.
3. **폴더 삭제 시 콘텐츠 처리 정책** — 미분류 이동 vs 하위 함께 삭제.

### 🟢 문서/정합 (비차단)
- 동의 이력 `agreed_terms`(`TB_ORDER_LOG`) → `TB_USER_AGREEMENT` 이관 · 마스킹 헬퍼 `lib/masking` 승격 · Figma 테넌트 ERD 보드(`tds3…`) `TB_OAUTH_CONFIG` 갱신 · `/health/db` verdict 문구 정정(`solsol_master`→`solsol`) · **`solsol-api` git 미초기화 지적은 §24에서 초기화 완료(`92ad1bd`)**.
