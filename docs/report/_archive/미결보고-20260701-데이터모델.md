# 미결·막힌 사항 보고 (2026-07-01, KST)

> 대상: 2026-06-30~07-01 데이터 모델·스키마 세션 및 인접 작업 중 **미해결/블로킹 항목만** 정리.
> 완료 내역은 `docs/history/history.20260630.md`(§8~27)·`history.20260701.md`(§10) 참조.

## 🔴 막힌 것 (blocked)

| 항목 | 상태·원인 | 우회/현황 |
| --- | --- | --- |
| **dev `/ops` 403** | `solsol-api/wrangler.toml`의 `APP_ENV=production` → 마이그레이션/시드가 프로덕션 안전가드에 막힘 | `--var APP_ENV:local` 오버라이드로 우회 중. **config 정리 미결**(local 환원 여부 결정 필요) |
| **DB time_zone 근본 미적용** | Hyperdrive가 세션 `SET time_zone`을 쿼리 간 유지 못 함(멀티플렉싱) → 서버 차원 UTC 강제 불가 | 컬럼을 **TIMESTAMP(내부 UTC)** 로 전환해 우회. 서버 UTC(파라미터그룹)는 **공용 dev 클러스터(타 프로젝트 DB 공존)** 라 보류 |
| **추가 테넌트 자동 프로비저닝** | 앱 `solsol` 유저에 `CREATE DATABASE` 권한 없음 → 신규 테넌트 스키마 자동 생성 경로 미확보 | 수동 생성만 가능. 전용 프로비저닝 계정 필요(B-7, prod OQ) |

## 🟡 미결 (pending)

- **`solsol-api` 앱계층 ↔ 새 스키마 불일치** — 이번 변경(`TB_SITE`·`TB_USER` 통합·`login_id`·비밀번호 인라인·`TB_CREDIT` `_cr`/FIFO·`TB_SITE_USER` 등)이 앱 코드에 미반영. `schema.master.ts` drizzle 정의 stale, 크레딧 FIFO 차감/멱등 로직 미구현.
- **인증 실연동 미완** — 소셜 5종 OAuth 키(OQ-OAUTH)·프론트↔API 결선·인증 QA 라운드. (회원모델 변경으로 기존 인증 코드 재작성 필요)
- **크레딧 단가(`unit_price`) 값 미정** — 모델은 종량제 확정, 값은 config Open(M-3).
- **기존 크레딧 데이터 백필** — 운영 데이터 발생 시 구 `TB_CREDIT_CHARGE` → `TB_CREDIT` 원장 이전 절차 필요(현재 dev 시드뿐이라 무해).
- **biz-legal** — 국외이전·고유식별정보 수집근거 미확정.

## ⏸ 배포 보류 상태 (참고)

- `solsol`(사용자단)·`solsol-admin`(관리자단): **mock 데모로 배포됨 → 실 API·시크릿 결선 전까지 비프로덕션**.
- `solsol-brand*`: 백엔드 미연동.

## 정리

- 즉시 해소 가능: **`wrangler.toml` APP_ENV 정리** 정도.
- 나머지는 대부분 **"실 API 결선" 트랙**(앱계층 스키마 정합 + OAuth 키 + 시크릿 주입)으로 수렴 → 이 트랙 착수 여부 결정 필요.
