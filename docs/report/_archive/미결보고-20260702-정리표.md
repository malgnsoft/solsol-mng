# 미결·막힌 사항 정리 (2026-07-01 작업 기준)

- **작성일**: 2026-07-02 (KST)
- **대상**: 2026-07-01 작업(이력 `docs/history/history.20260701.md` §1~§24) + 2026-07-02 사용자단 목업 수정 세션
- **성격**: 미결(후속 개발)·막힌(오너/담당자 액션 대기) 항목 취합. 완료 항목은 이력 참조.

> **가장 시급한 결정 3**: ① **PG staging-gate 해제**(실결제 활성화 여부) ② **K-4 브랜드 색상 확정**(반응형·색상 패스 선행) ③ **DBA 브랜드 운영자 시드 적용**(brand-admin 실전환 잠금 해제).

---

## A. 오너 결정·승인 대기 (막힘 — 비가역/비용/방향)

| 항목 | 내용 | 출처 |
|---|---|---|
| **PG(결제) staging-gate 해제** | 크리에이터·브랜드 결제·환불·정산·구독·크레딧이 `TODO(staging-gate)`로 실행 차단(실 PG/토스 호출 0). 해제=실결제 비가역(CMP-06) → 승인 필요 | §11·§24·§15 |
| **「수정 지시서」 K-1~K-5** | K-1 관리자 타인조회 휴대폰 마스킹 정책 / K-2 관리자단 "PC권장" 반응형 범위 / K-3 screenList↔00화면목록 SoT 단일화 / **K-4 브랜드 색상 확정**(쏠쏠 인디고 vs 현재 퍼플) / K-5 이메일변경 인증코드 표준값(10분 권고) | 수정 지시서 §5 |
| **R2 버킷(업로드)** | 첨부·이미지·아바타 업로드가 스텁 — 버킷·배선 미완 | §24 |
| **refresh 쿠키 크로스도메인** | `.pages.dev↔.workers.dev` 서드파티 쿠키 — 동일도메인 정렬/프록시 전략 결정 필요 | §24 |

## B. 담당자 직접 실행 대기 (시드·핸드오프)

- **브랜드 운영자 시드 미적용** — `seed.admin.FINAL.sql`(실 PBKDF2 해시) 준비 완료. DBA가 자기 env로 Aurora master `solsol`.TB_USER에 직접 적용 필요 → **미적용이라 `solsol-brand-admin` 실 API 전환 보류**(목업 배포 무손상). (§16·§17)
- **테넌트 `TB_OAUTH_CONFIG` 행 시드** — 소셜 로그인 활성화 전제, 미시드. (§11-b)
- **시드 계정 초기 비번 변경** — 브랜드 `superadmin`/`support` 최초 로그인 후 즉시 변경 필요. (§22)

## C. 미커밋·미배포 잔여

- **`solsol-brand-admin`** — `useRuntimeConfig(event)` 수정분이 working tree에만(배포됨·미커밋) → origin 커밋 필요. (§22)
- **`solsol` 인증 프론트** — 커밋·푸시만, **배포 보류**(소셜 OAuth 키·실연동·인증 QA 후). §6 회원모델 변경으로 인증 코드 재작성 필요. (§2)
- **사용자단 목업 FR-1~14 (2026-07-02 세션)** — 로컬 수정·`pnpm build` GREEN·qa 게이트 16/16 GO 완료, **커밋/배포 미실행**(요청 대기).

## D. 실연동 개발 후속 (현재 폴백 유지)

- **크리에이터 관리자단**: 조회형 실전환 완료. 남은 것 = **상세(detail) → 쓰기형 CRUD(CSRF 방어 BFF) → 목록 필터/정렬 서버위임**. 멤버십 등급·notification 매트릭스·site.meta **키 불일치로 목 유지**(날조 0). (§18·§23)
- **크리에이터 사용자단**: 실 테넌트 프로비저닝(현재 `dev` 데모), 강의실·수료증 스키마 확정 후 잔여 조회 결선. (§24)
- **브랜드 사용자단**: 인증·쓰기 실연동 미완 — **DB/계정 시드 + 계약 프리즈** 전제. (§19)
- **solsol-api 백엔드 잔여**: `/api/auth/login` 경로 정렬·계정열거(F-2)·레이트리밋(F-3)·공개경로(F-5)·`/health/db` verdict 문구(`solsol_master`→`solsol`)·`SEED_ADMIN_PASSWORD` secret화. (§18·§11)

## E. 보안·인프라 잔여

- **CF WAF 레이트리밋** — 로그인 엔드포인트(brand-api·brand-admin BFF·admin) 엣지 레이트리밋 미적용(인메모리는 Isolate 간 미공유). (§17·§22)
- **localStorage access 토큰**(XSS·중), **R2-1 가입 UNIQUE 제약**·D-3 주문 스냅샷 컬럼 후속. (§24)
- **마스킹 헬퍼 lib 승격**(`maskAccount`·`maskBizNo`)·동의이력 `TB_USER_AGREEMENT` 이관. (§11-e)

## F. 사용자단 목업 — 이번 지시서에서 남긴 것 (2026-07-02 세션)

- **COM-1 모바일 반응형** — 오너 결정으로 **다음 전용 패스**(전 페이지, 공수 큼).
- **COM-3 브랜드 색상** — **K-4 확정 대기**(색/토큰 무변경, 이름 텍스트 monzo→쏠쏠만 교체).
- **payment-restricted 정책 방향**(결제유도 vs 차단화면)·**2-a/2-b 구독 컨펌 모달** — 지시서 FR 범위 밖 미착수(멤버십은 버튼만 배치).
- 검증 정본: `docs/dev-validation/mockup-fr01-round1.md`(라운드1 격차) → FR-1~14 반영 완료.

## G. 문서/소소

- Figma 테넌트 ERD 보드(`tds3…`)에 `TB_OAUTH_CONFIG` 제자리 갱신. (§11-d)
- `docs/DEPLOY.md`의 TOSS·Phase1 어댑터 TODO. (§22)

---

### 다음 액션 후보 (지정 시 총괄이 오케스트레이션)
1. 사용자단 목업 **배포**(커밋·푸시·배포·이력 일괄) — 요청 대기 중.
2. 사용자단 목업 **반응형(COM-1) 패스** 착수 — K-4 색상 확정과 병행 권장.
3. PG staging-gate / R2 / 쿠키 전략 등 A항목 결정.
4. DBA 브랜드 운영자 시드 적용 → brand-admin 실전환.
