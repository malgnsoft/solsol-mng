# 미결·블로커 정리 — 2026-07-01 작업 기준

> 작성일: 2026-07-02 (KST) · 출처: `docs/history/history.20260701.md` §1~§24 + 각 레포 메모리
> 성격: 어제(2026-07-01) 작업에서 **미결·보류·게이트** 상태로 남은 항목 취합. 배포·완료 항목은 제외.

두 부류로 나눈다 — **ⓐ 교차(공통) 블로커**(여러 앱에 공통으로 걸림) / **ⓑ 프로젝트별 잔여**.

---

## ⓐ 교차(공통) 블로커 — 실서비스 전환 선결

| 블로커 | 영향 범위 | 해제 조건 / 소유 |
|---|---|---|
| **결제·정산·구독·크레딧·발송 = staging 게이트로 실행 차단** (`// TODO(staging-gate)`, 실 PG/TOSS 호출 0) | solsol-api · solsol-brand-api → (solsol · solsol-admin · brand 전부) | **오너 확인 후** TOSS 실키 주입·게이트 해제(비가역 CMP-06) |
| **DB·계정 시드 미실행** — 이 실행 환경에 Aurora 직접 쓰기 경로 없음 | brand-api 운영자 시드(`seed.admin.sql`) 미적용 → brand-admin 실전환 보류 / brand-api DB 비어있음(news·plans) → 브랜드 사용자단 실데이터 미노출(mock 폴백 중) | **DBA가 자기 env로** 핸드오프 SQL 적용 |
| **refresh 쿠키 크로스도메인** (`.pages.dev ↔ .workers.dev` 서드파티 쿠키) | solsol(결선됨) · 브랜드 실연동 시 | 동일도메인 정렬 or Pages 프록시 전략 (오너) |

---

## ⓑ 프로젝트별 잔여

### solsol (크리에이터 사용자단) — §23·§24 실 API 결선 go-live 완료
- 오너 확인 대기:
  1. **PG staging-gate 해제** (실결제·실환불)
  2. **R2 버킷** (첨부·이미지·아바타 업로드 = 현재 스텁)
  3. **refresh 쿠키 크로스도메인** 전략
  4. **localStorage access XSS** (중)
  5. **실 테넌트 프로비저닝** (현재 `dev` 데모 테넌트 1개)
- §2 잔재: 소셜 OAuth 키 미주입 → 소셜 로그인 비활성

### solsol-admin (크리에이터 관리자단) — §18·§22 실연동 배포 완료
- 백엔드(solsol-api) 라운드1 잔여: `/api/auth/login` 경로 정렬 · **F-2 계정열거** · **F-3 레이트리밋** · F-5 공개경로 · `/health/db` verdict · **`SEED_ADMIN_PASSWORD` Pages secret화·로테이션**
- TOSS 결제 시크릿 · Phase1 미매핑 도메인 어댑터 (`DEPLOY.md` TODO)
- 잔여(하): 페이지빌더 실 DnD · 이미지 업로드/크롭 · toss 카드등록 · 카카오 연결

### solsol-api (크리에이터 백엔드)
- 결제류 staging 게이트 (→ ⓐ)
- **소셜 OAuth: `TB_OAUTH_CONFIG` 행 시드 필요** (소셜 로그인 활성화 전제)

### solsol-brand (브랜드 사용자단) — §13·§19·§21 배포 완료
- **인증·쓰기 실연동 미완** (login/signup/account/sites/subscriptions/payments) — 전제: brand-api **DB·계정 시드 + 계약 프리즈 + 보안 게이트**(서버 인증/세션·IDOR·인증코드 서버검증·레이트리밋·PAN 마스킹·정보통신망법). 현재 **공개 읽기(news·plans)만 실연동 + mock 폴백**.
- **[TEST-ONLY] 인증번호 완화(§21) → 운영 전 반드시 서버검증 원복**
- 오너 판정 대기 (1px 충돌):
  - **color-contrast** 색 조정 (핸드오프 색상 상속)
  - **모바일 리플로우 반응형** (현재 BrandCanvas 고정캔버스 zoom 축소)
  - **C01 약관상세 모달** 신설 여부 (현재 전체페이지 이동)

### solsol-brand-admin (브랜드 관리자단) — §16·§17 목업 모드만 배포
- **실전환 보류**: `NUXT_API_BASE` 미설정 — brand-api **운영자 시드 대기**(→ ⓐ). 시드 적용 → admin 로그인 200 재검증 → 실전환

### solsol-brand-api (브랜드 백엔드) — §15·§17 실배포 완료
- **운영자 시드 미적용** (DBA 핸드오프 SQL 대기, `superadmin` 로그인 401)
- **TOSS 미주입** (staging-gate) · **CF WAF** 미설정
- staging 게이트: 결제·구독·카드등록·프로비저닝·웹훅 골격 + TODO

### solsol-admin/mockup (관리자단 목업) — §14 동결해제·검증 라운드2
- **검증 라운드2 10/10 NO-GO** (정책 충실도 낮은 초기 축약본) — 근본원인 8계열(마스킹유틸·RBAC·비가역게이트·인증정책·정책정합·핵심요소·정통망법·공통상태). 결함표 `docs/dev-validation/AD01-mockup-round2.md`
- 반영: AD-1/2/3(무게이트)만. **보류(DJ 선행 컨펌)**: AD-4(마스킹·K-1) · AD-5/COM-3(브랜드색·K-4) · COM-1(반응형·K-2) · COM-2(화면ID SoT·K-3)
- **P0 공통 인프라**(마스킹 유틸·비가역 컨펌 모달·RBAC 골격·공통 상태 컴포넌트) 착수 시 다수 blocker 동시 해소

---

## 가장 시급한 다음 3가지 (권장 순서)

1. **DBA에게 brand-api 시드 SQL 적용 요청** → brand-admin 실전환 + 브랜드 사용자단 실데이터 노출 **동시 해소**.
2. **오너 결정: PG(TOSS) staging-gate 해제 여부** → solsol · brand 전 결제 흐름 실동작.
3. **브랜드 사용자단 오너 판정 3건**(color-contrast · 모바일 반응형 · C01 모달) 확정 → 잔여 UI 마무리.

---

## 부록 — 참고 문서

- 작업 이력: `docs/history/history.20260701.md` (§1~§24) · 인덱스 `docs/history/README.md`
- 검증 결함표: `docs/dev-validation/` (AD01-mockup-round2 · brand-site-round1 · brand-admin-round1 등)
- 데이터모델 정본: `docs/data-model/ERD.md`
