# FR01 사용자단 실 API 결선 — 미결·막힌 사항 보고

| 항목 | 내용 |
|------|------|
| 작성일 | 2026-07-02 |
| 대상 | 크리에이터 사용자단(solsol / FR01) ↔ 실 API(solsol-api) 결선 |
| 기준 작업 | 2026-07-01 이력 §24 (전체 결선 A/B/C + 안전 go-live) |
| 상태 | go-live 완료(https://solsol.pages.dev ↔ 실 Aurora dev 테넌트) — 아래는 잔여 |

> 어제(2026-07-01)는 **FR01 사용자단 48화면 구축·배포 → 실 API(solsol-api) 전체 결선(A 조회·인증 / B 쓰기 / C 결제 승인대기) → 안전 go-live**까지 완료했다. 본 문서는 그 이후 **남은 미결·막힌 사항**을 정리한다.

---

## 🔴 오너 확인 필요 (비가역·비용·비즈니스 — 임의 진행 불가)

| # | 항목 | 막힌 이유 | 필요 결정 |
|---|------|----------|----------|
| 1 | **PG staging-gate 해제** | 결제·구독이 `{pending:true}` **승인대기**까지만 동작. 실결제·실환불은 비가역(CMP-06) | toss 웹훅·staging 선검증 후 **해제 승인** |
| 2 | **R2 버킷 생성** | 첨부·이미지·아바타 업로드가 스텁(`url:null` / 501). `R2_PUBLIC_BASE` 미설정 | 버킷·공개 도메인 **생성 승인**(비용·외부 리소스) |
| 3 | **실 테넌트 프로비저닝** | 현재 `dev` 데모 테넌트 1개뿐(상품·게시판 등 시드 데이터만 존재) | 커스터머별 사이트·도메인 매핑 결정(도메인 매핑 시 host 기반 테넌트 해석 자동) |

## 🟢 기술 미결 (오너 불필요 — 승인 시 즉시 재진행 가능)

| # | 항목 | 현황 |
|---|------|------|
| 4 | **남은 조회 실연동** | 강의실·대시보드·라이브·수료증·내상품·FAQ는 "스키마 미확정"으로 **mock 폴백 유지**. 백엔드 엔드포인트(`/api/classrooms/:id`·`/dashboard`·`/api/live-rooms/:id`·`/api/me/certificates`·`/api/my-products`·`/api/faqs`)는 실재 → 응답 필드 매핑만 하면 실 API 전환 가능 |
| 5 | **refresh 쿠키 크로스도메인** | `solsol.pages.dev ↔ *.workers.dev` 서드파티 쿠키 정책(Safari ITP·Chrome)으로 **세션 갱신(refresh) 실패 위험**. 동일오리진 프록시(Pages Functions) 또는 커스텀 도메인 정렬로 해소 |

## 🟡 후속 (경/중)

| # | 항목 | 현황 |
|---|------|------|
| 6 | localStorage access 토큰 | XSS 시 탈취 가능(중). 단명(15분)이나 메모리 보관/CSP 강화 후속 검토 |
| 7 | 가입 멱등 UNIQUE 제약 | Hyperdrive stale-read 중복가입은 애플리케이션 레벨(재조회·INSERT 멱등)로 완화 완료. 전용 UNIQUE 제약/락은 후속 |
| 8 | orders 스냅샷 컬럼 | 내주문 표시는 `TB_ORDER_ITEM`↔`TB_PRODUCT` 조인 폴백으로 해소. `product_name` 스냅샷 채움은 후속 |

---

## 참고 — 어제 산출물·커밋

- **프론트** `solsol` `f56a8b1`(malgn) → **https://solsol.pages.dev** (실 API 연결, `apiBase`+`tenantSlug=dev`)
- **백엔드** `solsol-api` git 초기화 `92ad1bd` → 워커 재배포 `ec5c247f` → **https://solsol-api.malgnsoft.workers.dev** (CORS에 `solsol.pages.dev` 추가·구버전 임의오리진 취약점 해소·dev 테넌트 도메인 시드 24테이블)
- **이력** `ae8bcd6` (§24) · **검증기록** `docs/dev-validation/wiring-phaseA-round1.md`·`wiring-phaseA-round2.md`·`wiring-phaseBC-round1.md`

## 요약

- 실질적 남은 **개발** 항목 = **4·5** (기술, 오너 불필요, 즉시 착수 가능).
- 실서비스 오픈 전 **결정** 항목 = **1·2·3** (오너: 실결제 해제 / R2 / 실 테넌트).
- 결선 자체(조회·인증·쓰기·결제 승인대기)는 로컬 실 API E2E 전 Phase GO(blocker 0)로 검증 완료.
