# 마이페이지 서브라운드 5c — QA 검증 결함표 (round1)

- **대상**: FR01 마이페이지 5c — 내게시글(0301-108) · 1:1문의하기(0301-109) · 문의내역(0301-110) · 문의상세(0301-111) · 프로필(0301-112) + 모달 _pu01/02/03
- **정본(읽기 전용)**: `docs/validation/01_customer-front.md`(0301-108~112 + _pu01~03) · `04_정책요약.md`(SUP-01/02·AUTH-10/15·C-1·C-6·CMP-06·LRN-20) · `05_정책설계서`(C-1 닉네임 2~15)
- **검증 대상 코드**: `/Users/dotype/Projects/solsol/app/`
- **검증일**: 2026-07-01 · **검증자**: QA(FR01)
- **절대 규칙 준수**: 정본·구현 파일 **무수정**. 본 결함표만 신규 작성.

---

## 1. 커버리지 (화면ID 5건 + _pu01/02/03)

| 화면ID | 화면 | 구현 파일 | 상태 | 주석 |
|--------|------|-----------|------|------|
| S-FR01-0301-108 | 내 게시글 | `app/pages/mypage/posts.vue` | 있음 | 3탭(후기/커뮤니티/게시판)·탭별 필터/정렬·별점정렬(후기탭)·검색·10개+더보기·빈상태 |
| S-FR01-0301-109 | 1:1 문의하기(작성) | `app/pages/mypage/inquiry/write.vue` | 있음 | 라우트 `/mypage/inquiry/write`. 유형4종·진입경로 자동입력·이메일 수정불가·에디터 50MB |
| S-FR01-0301-110 | 1:1 문의내역(목록) | `app/pages/mypage/inquiry/index.vue` | 있음 | 상태요약3종·검색·리스트·더보기(⋮)삭제·컨펌·빈상태 |
| S-FR01-0301-111 | 1:1 문의 상세 | `app/pages/mypage/inquiry/[id].vue` | 있음 | 수정불가(삭제만)·관련정보·첨부·CommentThread 답글·종료시 비활성 |
| S-FR01-0301-112 | 프로필(설정) | `app/pages/mypage/profile.vue` | 있음 | 이미지/이메일/닉네임/휴대폰/이름/연동SNS/마케팅동의/저장 |
| S-FR01-0301-112-P01 | 프로필 설정 모달 | `app/components/mypage/ProfileImageModal.vue` | 있음 | jpg/png·1:1 프레임·완료 비활성→활성·토스트 |
| S-FR01-0301-112-P02 | 닉네임 변경 모달 | `profile.vue` 내 AppModal + `components/auth/NicknameField.vue` | 있음 | 2~15자·중복체크·완료/중복 토스트 |
| S-FR01-0301-112-P03 | 계정 탈퇴 모달 | `profile.vue` 내 AppModal(3-step) | 있음 | 비가역 안내3종·사유 셀렉트·상세2자↑·이중컨펌·자동결제중지 |

**커버리지 판정**: 5화면 + _pu01/02/03 = **8/8 존재. 누락 0 · 부당 추가 0.**
- 보조 확인: `useInquiry.ts`(신규) 존재 · `CommentThread.vue` 옵셔널 확장(allowReport/allowNestedReply) 존재.

---

## 2. 결함표

`화면ID | 검증축 | 심각도 | 현상 | SoT근거 | 제안`

| # | 화면ID | 검증축 | 심각도 | 현상 | SoT근거 | 제안 |
|---|--------|--------|--------|------|---------|------|
| D-1 | 0301-108 | 상태/필터 | **중** | 커뮤니티/게시판 탭의 **2-1 필터(커뮤니티명/게시판명 select)** 가 렌더링만 되고 리스트에 **미적용**. `filteredPosts`(posts.vue:203~209)는 정렬만 처리하고 `filter.value`를 참조하지 않음. 상품후기 탭은 `filteredReviews`에서 정상 적용(posts.vue:178~200). | 01 §0301-108 2-1 "탭별 구성…게시판명 1~N" · P-44 "탭별 필터(…게시판명)" | `filteredPosts`에 `if (filter.value!=='all') list=list.filter(...)` 추가. mock 옵션이 all+1개뿐이라 실동작 영향은 제한적이나 계약 반영 시 필수. |
| D-2 | 0301-108 | 상태(검색) | 하 | 커뮤니티/게시판 탭 검색은 `applySearch`→`fetchData`(서버 q 재조회)로 처리되나, **정렬/필터 변경 시에는 재조회 없이 로컬 정렬만** 반영되어 서버측 q 필터와 로컬 정렬 기준이 혼재. 현재 mock 범위에선 무결하나 실API 전환 시 페이징/정렬 일관성 리스크. | 01 §0301-108 2 "다중 필터/정렬 동시 적용·실시간 변경" | 실API 전환 시 정렬/필터도 서버 파라미터로 통합(계약 시). 현재는 mock 폴백 명시(주석 존재)로 수용 가능. |
| D-3 | 0301-108 | 카피 | 하 | 후기 삭제 컨펌 카피 "작성하신 후기를 삭제하시겠어요?"(posts.vue:426)는 정본 상품후기 삭제 컨펌 "상품 후기를 삭제하시겠어요?"(01 §0102-002 9-c, p071)와 미세 문구 차이. | 01 §0102-002 17.3-a "상품 후기를 삭제하시겠어요?" | 원본 후기 영역과 동일 카피로 통일 권장(P-14 "원본 영역과 동일"). |
| D-4 | 0301-112 | 상태/카피 | 하 | 휴대폰 마스킹 규칙이 정본 명시 없이 자체 규칙(`010-****-1234`, profile.vue:50~55) 채택. 정본은 P-32를 `[→ 강테크 확인]`으로 미확정 표기. C-6은 "본인화면 식별정보 비마스킹 허용, 단 휴대폰은…"으로 휴대폰 처리가 화면설계서(4-1 "마스킹 P-32 적용")와 C-6(본인 비마스킹) 간 경합 여지. | 01 §0301-112 4-1 "마스킹 P-32 적용 대상 `[→ 강테크 확인]`" · 04 C-6 | 팀장/강테크 컨펌 대기 항목. 현 구현(마스킹)은 화면설계서 4-1을 우선 채택한 합리적 선택 — **정책 확정 시 재확인** 필요(결함 아닌 미확정 이관). |
| D-5 | 0301-112 P02 | 카피 | 하 | 닉네임 중복 토스트가 두 곳에서 문구 상이: profile.vue:125 "이미 사용중인 닉네임 입니다. 다른 닉네임을 입력해 주세요"(정본 일치) vs NicknameField.vue:65 인라인 "이미 사용 중인 닉네임입니다."(축약). 표시 위치가 달라(모달 토스트 vs 필드 인라인) 실사용엔 문제없으나 문구 일관성만 미세 차. | 01 §112-P02 2-b(p355) "이미 사용중인 닉네임 입니다 / 다른 닉네임을 입력해 주세요" | 인라인/토스트 문구 통일 권장(선택). |

**상(blocker) 결함: 0건.**

---

## 3. 핵심 검증 결과 (정본 대조)

### 3.1 SUP-01 상태머신 (문의) — ✅ 충족
- `useInquiry.ts:29` `InquiryStatus = 'pending' | 'in_progress' | 'answered' | 'closed'` 4종 정의.
- 라벨(105~116): 답변대기/답변중/답변완료/문의종료 정확. 배지 색(124~135)은 **색+라벨 병기**(색만 의존 금지 준수).
- **문의 수정 불가·삭제만**: 목록(index.vue:243 삭제만)·상세(`[id].vue`:153~160 더보기 [삭제]만) — 수정 진입 없음. ✅
- **종료(closed) 시 답글 비활성**: `[id].vue`:49 `canReply = status!=='closed'` → CommentThread `:can-reply` 전달 + 안내문(`[id].vue`:198). CommentThread:68~70 `showRootInput`이 `canReply`로 게이트. ✅
- **관리자 답변 편집 불가**: mock 관리자 답변 `isMine:false`(useInquiry:242) → CommentThread:382 `v-if="comment.isMine"`로 수정/삭제 미노출 + `allowReport:false`로 신고도 미노출. 학습자 본인 답글만 수정/삭제. ✅
- 상태 요약(index.vue:54~62·120~134): 답변대기/답변중/답변완료 3종 개수(closed 제외) — 정본 §4 "답변대기/답변중/답변완료 상태별 개수" 부합. ✅
- 유형 4종(useInquiry:91~102): 상품/결제(구매및환불)/신고하기/기타 — 정본 부합. ✅

### 3.2 진입경로 자동입력 (109) — ✅ 충족
- `write.vue:46~83` `applyEntryQuery`: `?type=payment&ref=<주문번호>&product=<상품명>`(결제) / `?type=report&refType=post|comment&refId&refTitle&boardKind`(신고). 기본진입=유형 '상품'·관련정보 없음. 정본 1-1 a/b/c/d 전부 대응.
- 이메일 자동·**수정불가**: `write.vue:250~260` 대표 소셜 이메일 read-only div(input 아님), 본인 비마스킹(C-6). ✅
- 에디터 50MB: `write.vue:27,267~274` limit-mb=50. ✅

### 3.3 계정 탈퇴 이중컨펌 (112-P03 · AUTH-10/CMP-06) — ✅ 충족 (상 게이트 통과)
- **3단계 이중 컨펌**: profile.vue:157 `withdrawStep 1→2→3`. Step1=비가역 안내(3종), Step2=사유 셀렉트+상세, Step3=최종 컨펌("계정을 정말 탈퇴하시겠어요?"). 실행은 **Step3에서만**(`handleWithdraw`:191~213, step<3이면 다음 단계로만 진행 → 자동진행 0). ✅
- **비가역 경고 카피 3종**(profile.vue:420~422): ① 이용내역·학습기록 모두 삭제·서비스 이용 종료 ② 구독 상품 자동결제 탈퇴와 함께 중단 ③ 동일 이메일 재가입해도 기존 데이터 복구 안 됨 — 정본 P-47/AUTH-10 완전 일치.
- **사유 필수 + 상세 2자↑**: `withdrawFormValid`(172~174) = 사유 선택 + `detail.trim().length>=2`(공백만 무효). Step2 [다음] 및 최종 버튼 disabled 게이트(468~475). ✅
- **자동결제 중지 고지**: Step3 카피 "자동 결제가 중지됩니다"(461) + 실행 payload. ✅
- 최종 컨펌 카피 "계정을 정말 탈퇴하시겠어요?"(460) = 정본 3-a(p358) 일치. 완료 토스트 "계정 탈퇴가 완료되었습니다"(206) = 정본 3-b(p360) 일치.

### 3.4 닉네임 2~15 (C-1 확정) — ✅ 충족 (상 게이트 통과)
- `NicknameField.vue:35~40` `validate`: `length<2 || length>15` → "닉네임은 2~15자로 입력해 주세요." · maxlength=15(89) · placeholder "2~15자"(15) · 카운터 "{n}/15자"(99).
- 중복체크: 실시간 debounce(42~76) → 중복 시 valid=false. 05 §C-1 "전 영역 2~15자 단일 규칙" 위반 없음. ✅

### 3.5 마스킹 (C-6) — ✅ 충족 (본인 비마스킹)
- 프로필 이메일(profile.vue:254)·닉네임(268)·이름(298): **비마스킹**(본인 화면, C-6 허용). ✅
- 문의하기 이메일(write.vue:257): 비마스킹. ✅
- 휴대폰(profile.vue:50~55): 마스킹 적용(`010-****-1234`) — 화면설계서 4-1 "P-32 마스킹" 채택. C-6과 경합 여지 있으나 정책 미확정 이관(D-4).

### 3.6 라우트 정합 (0301-109 write) — ✅ 충족
- 파일 존재: `app/pages/mypage/inquiry/write.vue` → 라우트 `/mypage/inquiry/write` 생성.
- 링크 해소 확인:
  - 목록 index.vue:113,190 `to="/mypage/inquiry/write"` → 해소 ✅
  - 5b 결제내역 `payments.vue:173~176` `path:'/mypage/inquiry/write', query:{type:'payment',ref,product}` → write.vue:50~62가 정확히 파싱 ✅
- **깨진 링크 0건.**

### 3.7 회귀 (CommentThread 옵셔널 확장) — ✅ 무손상
- `allowReport`·`allowNestedReply` 둘 다 `withDefaults`로 **기본값 true**(CommentThread:48~52). 커뮤니티/게시판 default 호출부는 props 미전달 시 **기존 동작(신고 노출·3계층 대댓글) 그대로 보존**.
- 문의 상세만 `allow-report=false`·`allow-nested-reply=false` 명시(`[id].vue`:205~206) → 문의 답글=1계층·신고 미노출. `showRootInput`(68~70)이 문의는 canReply로, 커뮤니티는 allowNestedReply(true)로 게이트 → 커뮤니티 로그인 입력 노출 회귀 없음.
- `allowSecret`도 기본 false로 premium/notice 회귀 없음(기존 R4 유지). **회귀 0.**

### 3.8 05 확정 6건 대조
| 05 항목 | 관련 여부 | 결과 |
|---------|-----------|------|
| ① 닉네임 2~15자 단일 | 해당(112-P02) | ✅ 준수 |
| ② 무료체험 미운영 | 무관 | - |
| ③ 강사 RBAC | 무관 | - |
| ④ 유효시간 | 무관 | - |
| ⑤ 비밀번호 문구 | 무관(소셜 로그인·PW 미사용) | - |
| ⑥ 쿠폰 정액 only | 무관 | - |
- 위반 0건.

### 3.9 typecheck
```
cd /Users/dotype/Projects/solsol && pnpm exec nuxt prepare; pnpm typecheck
→ EXIT=0, error TS 0건
```
- WARN "Duplicated imports BoardAuthorType"는 **기존(baseline) 경고**(5c 신규 도입 아님, useBoard↔boardFormat 중복 재노출) — 타입 에러 아님. 신규 오류 0.

---

## 4. 종합 판정

**판정: GO** (조건부 — 배포 게이트 통과)

- **상(blocker) 0건** → 종료조건 충족.
- **탈퇴 비가역 이중컨펌·비가역 카피·자동진행0** ✅ / **닉네임 2~15 규칙** ✅ / **본인 비마스킹(C-6)** ✅ / **SUP-01 상태머신·수정불가·종료시 답글 비활성** ✅ / **라우트 정합(write 링크 해소)** ✅ / **CommentThread 회귀 0** ✅ / **typecheck 0** ✅.

### 우선 보완 (배포 후 또는 계약 반영 시)
1. **D-1(중)** — 내게시글 커뮤니티/게시판 탭 2-1 필터를 `filteredPosts`에 실제 적용(admin-developer/frontend-developer 위임). mock 옵션이 제한적이라 실서비스 영향은 낮으나 정본 2-1 명시 기능이므로 실API 계약 전 반영 권장.
2. **D-4(하·미확정 이관)** — 휴대폰 마스킹 정책(화면설계서 4-1 P-32 vs C-6) 팀장/강테크 컨펌 후 재확인.
3. **D-3/D-5(하·카피)** — 후기 삭제 컨펌·닉네임 중복 문구를 원본 영역 정본 카피와 통일(선택).

> D-1은 중이므로 배포 게이트를 막지 않으나, "다중 필터 동시 적용" 정본 요건의 부분 미구현이다. 담당 개발 역할에 회신 권장.
